---
title: Smooth Neural Point Processes via B-Splines
title_zh: 基于B样条的平滑神经点过程
authors: "Michele Bellomo, Riccardo Ramaschi, Alberto Dolara, Tomaso Aste"
date: 2026-07-23
pdf: "https://arxiv.org/pdf/2607.21098v1"
tags: ["query:tpp-es"]
score: 9.0
evidence: 提出使用B样条进行平滑的神经点过程，用于连续时间事件序列建模
tldr: 时序点过程（TPP）用于建模连续时间事件序列。现有神经TPP依赖精确负对数似然（NLL）计算，但通常只能建模补偿器而非强度函数，且训练时无法并行。本文提出直接参数化条件强度函数（CIF）为B样条基函数非负组合，系数由神经网络预测。该方法支持精确NLL、灵活架构、高效并行训练和自然平滑正则化。实验表明，在合成和真实数据集上计算效率和预测精度均优于基线。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.21098v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1441, \"height\": 439, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.21098v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1440, \"height\": 440, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.21098v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1065, \"height\": 480, \"label\": \"Table\"}]"
motivation: 现有神经TPP方法要么需要数值积分，要么限制架构且训练无法并行，缺乏直接建模CIF且高效可并行的方案。
method: 将CIF表示为B样条基函数的非负组合，系数由神经网络输出，从而支持精确NLL计算和并行训练。
result: 在合成和真实数据上，所提方法相比基线提升了计算效率和预测准确性。
conclusion: 通过B样条参数化CIF，实现了高表达力、可并行、带平滑正则的神经TPP模型。
---

## 摘要
时间点过程（TPPs）为连续时间中的事件序列建模提供了一个通用且灵活的框架。神经网络已被成功用于以高度表达性和数据驱动的方式建模TPPs。神经TPPs通常通过极大似然估计（MLE）进行训练，最小化负对数似然（NLL），该似然依赖于条件强度函数（CIF）及其在时间上的积分（补偿器）。最近的神经TPP方法能够在无需数值积分的情况下精确计算NLL。然而，这些方法通常直接建模补偿器而非CIF，对神经网络架构施加约束，并且在训练中计算成本较高，因为事件对NLL的贡献是顺序评估而非并行评估。在这项工作中，我们提出了一种新颖的神经TPP模型，该模型将CIF直接参数化为B样条基函数的非负组合，其系数由神经网络预测。这种公式能够精确计算NLL，在神经架构中保持完全灵活性，允许训练期间的高效并行化，并通过二阶导数的平方积分自然地支持CIF平滑正则化。在合成和真实数据集上的实验表明，与参考的神经TPP基线相比，本方法在计算效率和预测准确性上均有提升。

## Abstract
Temporal point processes (TPPs) provide a general and flexible framework for modeling sequences of events in continuous time. Neural networks have been successfully employed to model TPPs in a highly expressive and data-driven way. Neural TPPs are typically trained via Maximum Likelihood Estimation (MLE) by minimizing the negative log-likelihood (NLL), which depends on both the conditional intensity function (CIF) and its integral over time, the compensator. Recent neural TPP approaches enable exact evaluation of the NLL without numerical integration. However, these methods typically model the compensator rather than the CIF directly, impose constraints on the neural network architecture, and are computationally expensive during training, as event contributions to the NLL are evaluated sequentially rather than in parallel. In this work, we propose a novel neural TPP model that directly parametrizes the CIF as a non-negative combination of B-spline basis functions, whose coefficients are predicted by a neural network. This formulation enables exact evaluation of the NLL, preserves full flexibility in the neural architecture, allows efficient parallelization during training, and naturally supports CIF smoothness regularization through the integrated squared second derivative. Experiments on both synthetic and real-world datasets show improved computational efficiency and predictive accuracy compared to the reference neural TPP baseline.

---

## 论文详细总结（自动生成）

### 论文核心总结：Smooth Neural Point Processes via B‑Splines

#### 1. 核心问题与整体含义（研究动机和背景）
- **背景**：时间点过程（TPP）用于建模连续时间事件序列。神经TPP通常通过最大似然估计（MLE）训练，依赖条件强度函数（CIF）及其积分（补偿器）。精确计算负对数似然（NLL）需要避免数值积分。
- **现有方法局限**：近期方法（如Omi et al. 2019）通过建模补偿器而非CIF来精确计算NLL，但必须对网络施加单调性约束（如全连接层权重非负），导致：
  - 架构灵活性受限（无法自由使用Transformer等）；
  - 训练和推理**必须顺序计算**每个事件的时间点，无法并行，效率低下。
- **本文目标**：提出一种可直接建模CIF、允许任意神经网络架构、支持并行训练且能自然施加平滑正则化的神经TPP模型。

#### 2. 方法论：核心思想与关键技术细节
- **核心思想**：将CIF（关于事件间间隔 τ）参数化为**B样条基函数的非负组合**，系数由神经网络预测。
  - 公式：ϕ(τ) = ∑_{k=1}^{K} w_k B_k(τ)，其中 w_k ≥ 0（使用softplus输出），B_k为B样条基函数。
- **关键技术细节**：
  - **闭式积分**：补偿器 Φ(τ) = ∫₀^τ ϕ(s) ds = ∑ w_k I_k(τ)，其中 I_k(τ) 为B样条的积分，可预先精确计算。
  - **并行性**：CIF可在所有事件时间点上并行评估（一次前向传播输出所有时间点的样条系数），而非每次评估一个点。
  - **平滑正则化**：通过惩罚CIF的二阶导平方积分 P(ϕ) = ∫(ϕ''(τ))² dτ = wᵀ R w（R为预计算的稀疏矩阵）实现，并加入损失函数：L = −NLL + α P(ϕ)。
  - **多变量扩展**：为每个事件类型 m 分配独立的B样条基和系数，网络输出总系数，正则化为各类型加权和。
  - **预测**：利用随机时间变化定理的中位数估计器，通过求解 Φ(τ̂) = log(2) 得到下次事件时间，因样条系数已知，只需一次前向传播，后用二分法求解。

#### 3. 实验设计
- **数据集**：
  - **合成数据集**（7种）：平稳/非平稳Poisson、平稳/非平稳Renewal、Self-correcting、单指数核Hawkes、多指数核Hawkes。每个数据集100,000个时间点，80%/20%训练/测试。
  - **真实数据集**（2个）：Music (last.fm用户听歌历史，100个最活跃用户)、Meme (流行短语时序，50个最常用短语)。预处理同Omi et al.。
- **基准方法**：直接对比Omi et al. [14]的模型（全联接网络+单调约束+顺序计算）。
- **评估指标**：基于中位数估计器的平均绝对误差（MAE）——预测下次事件间隔与真实间隔的差异。此外，报告训练计算速度提升。
- **模型架构**：最小Transformer（嵌入64，4头注意力，1个解码器块，FFN维度64）。输入为最后20个事件的间隔和位置编码。CIF用20个内部结的B样条。Adam优化器，lr=0.001，batch=256，早停（patience=10），验证集20%训练数据，α在[0, 10^-3 ~ 10^-10]中调优。

#### 4. 资源与算力
- **训练环境**：Google Colab，单张**T4 GPU**。
- **训练时长**（合成数据集平均）：
  - 标准顺序模式：每epoch约1750 ms；
  - 本文提出的**并行多评估模式**：每epoch约150 ms（速度提升约12倍）。
- **未给出总训练时间**（如总epoch数或总小时数），但表明并行化显著降低单epoch时间。

#### 5. 实验数量与充分性
- **实验数量**：7个合成 + 2个真实数据集，共9组主要对比实验。另包含2个可视化分析（正则化效果图）。
- **充分性与公平性**：
  - 数据集与baseline均来自[14]，保证可比性。
  - 超参数α通过验证集选择（包括α=0），评估客观。
  - 但**仅对比一个基线方法**，未与其他神经TPP（如Neural Hawkes, FullyNN等）比较；且基线使用RNN，本文使用Transformer，架构差异可能对结果有影响。
- **消融实验**：未明确做消融（如不同K值、有无正则化），但通过α扫描展示了正则化影响。

#### 6. 主要结论与发现
- **预测精度**：在5/7个合成数据集、2/2个真实数据集上MAE优于Omi et al.，尤其在真实数据集和“Hawkes 1”合成数据上改进显著。
- **计算效率**：并行多评估策略带来约12倍训练加速，大幅提升可扩展性。
- **正则化有效性**：多数最优α非零，正则化有助于减少CIF振荡、提高泛化（图1/2展示）。

#### 7. 优点（方法或实验设计亮点）
1. **直接建模CIF**：无需数值积分，避免单调性架构约束。
2. **完全并行化**：支持Transformer等现代架构，可在整个序列上并行计算NLL，显著加快训练和推理。
3. **自然平滑正则化**：基于B样条的二阶导惩罚可预计算、稀疏、几乎零额外计算成本，有效控制过拟合。
4. **闭式积分**：补偿器精确可求，保证了NLL的精确性。
5. **预测便捷**：一次前向传播可得到整个CIF曲线和补偿器函数，无需多次网络调用即可求解下次事件时间。

#### 8. 不足与局限
- **额外超参数α**：引入了正则化系数α，调优尝试可能部分抵消计算效率增益，缺少自动调优策略。
- **规模验证不足**：仅在中等规模（100k点、100序列）数据集测试，未验证在大规模、多类型、长序列场景下的表现。
- **基线对比单一**：只与Omi et al. 对比，缺乏与近期其他并行神经TPP方法（如基于神经ODE、扩散模型等）的比较。
- **未报告优化环节细节**：如不同结数K对性能的影响、不同网络大小下的效果等。
- **潜在偏差**：使用Transformer与基线RNN直接对比，架构差异可能贡献了一部分性能提升，未控制变量。

（完）
