---
title: Trend and seasonality estimation for point-process time series
title_zh: 点过程时间序列的趋势与季节性估计
authors: "Daniel Gervini, Simon A. Kopischke"
date: 2026-05-21
pdf: "https://arxiv.org/pdf/2605.21884v1"
tags: ["query:tpp-es"]
score: 9.0
evidence: 点过程时间序列的趋势与季节估计
tldr: 点过程时间序列的趋势和季节成分估计是重要问题。本文针对服从对数高斯强度函数的双随机泊松模型，提出了计算简单的M估计量，并推导了其渐近正态性。通过模拟实验验证了有限样本下的良好性能，并应用于芝加哥Divvy共享单车数据，揭示了自行车需求的趋势和季节模式。该工作为点过程时间序列分析提供了新工具，具有理论价值和实际应用意义。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.21884v1/fig-001.webp\", \"caption\": \"Table 1: Simulation Results. Bias, standard deviation and root mean squared error for estimators of the baseline intensity function λ(u) and the trend c̃(t), for three different models: (i) Yt = 0, (ii) independent Yts, and (iii) autoregressive Yts.\", \"page\": 15, \"index\": 1, \"width\": 632, \"height\": 366}]"
motivation: 现有方法复杂且难以同时估计点过程中的趋势和季节成分，需要一种计算简单且理论可靠的估计方法。
method: 基于双随机泊松模型和log-Gaussian强度函数，构造M估计量，并推导其渐近分布。
result: 模拟表明估计量在有限样本下表现良好；实际数据应用成功识别了自行车需求的趋势和季节性。
conclusion: 本文提出的M估计量计算简便、理论可靠，为点过程时间序列的趋势和季节分析提供了有效解决方案。
---

## 摘要
本文介绍了点过程时间序列的趋势和季节性估计量。我们假设点过程遵循具有对数高斯强度函数的时间或空间双随机泊松模型。所提出的估计量是计算简单的M估计量。推导了它们的渐近分布，并通过模拟研究了它们的有限样本性能。作为实际数据应用的示例，我们研究了芝加哥市Divvy共享单车系统中自行车需求的模式。

## Abstract
This article introduces estimators of trend and seasonality for time series of point processes. We assume the point processes follow a temporal or spatial doubly-stochastic Poisson model with log-Gaussian intensity functions. The proposed estimators are computationally simple M-estimators. Their asymptotic distribution is derived, and their finite-sample performance is studied by simulation. As an example of real-data application, we study the patterns of bike demand in the Divvy bike-sharing system of the city of Chicago.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：如何对**点过程时间序列**（即一系列点过程观测值构成的序列，例如每天记录的事件时间集）中的**趋势**（如年度变化）和**季节性**（如周周期）进行有效估计。
- **研究动机**：现有点过程建模多聚焦于单次实现（单个时空过程），而对象导向数据分析视角下的重复点过程（例如将长期过程切片得到多个短期点过程）在统计建模上研究不足，且重复观测间往往存在时间依赖（非独立）。传统函数型时间序列方法无法直接处理点过程（计数随机且可能低）。需要开发针对点过程时间序列的趋势/季节性估计方法。
- **整体含义**：为分析诸如共享单车需求等每天产生事件数据的系统提供统计工具，理解内在的长期趋势和周期模式，以支持运营决策（如车辆调度）。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：假设每个点过程 \(X_t\) 服从**双随机泊松过程**（Cox过程），其强度函数 \(\Lambda_t(u)\) 为对数高斯过程，即：
  \[
  \log \Lambda_t(u) = \mu(u,t) + Y_t(u),
  \]
  其中 \(Y_t(u)\) 为零均值高斯过程（刻画随机波动，非可观测），\(\mu(u,t)\) 为均值函数。
- **趋势与季节性的加法分解**：
  - 仅趋势模型：\(\mu(u,t) = \mu(u) + c(t)\)，\(c(1)=0\) 确保可识别性。
  - 趋势+季节性模型：\(\mu(u,t) = \mu(u) + c(t) + s_{j(t)}(u)\)，其中 \(j(t)\) 表示季节指数（如星期几），\(\sum_{j} s_j(u)=0\)。
- **参数化与估计**：
  - 基函数表示：\(\mu(u)\) 和 \(s_j(u)\) 用样条基（如B样条），\(c(t)\) 用多项式基（中心化以适应约束）。
  - 采用**M估计**：使用工作似然函数（假设 \(Y_t=0\) 时的对数似然），最大化目标函数（公式(10)(11)）。该函数是凹函数，优化简便。
- **渐近理论**：
  - 在正则条件下（A1-A5），估计量具有一致性（定理1）。
  - 当 \(Y_t\) 独立时（A6），得到渐近正态分布（定理2），方差为“sandwich”形式，可通过经验得分矩阵估计。

## 3. 实验设计：使用了哪些数据集/场景、基准、对比方法

- **模拟实验**：
  - 仅考虑**趋势模型**（无季节性），参数参照实际共享单车数据。
  - 三种 \(Y_t\) 设定：
    (i) \(Y_t=0\)（工作模型）；
    (ii) 独立 \(Y_t\)（因子模型，\(Y_t(u)=Z_t \zeta(u)\)，\(Z_t \sim N(0,\sigma^2)\)）；
    (iii) 自回归 \(Y_t\)（AR(1)过程，\(a=0.7\)）。
  - 样本量 \(n=50,100,300\)，每个场景重复1000次。
  - **无对比方法**，仅评估自身估计量的偏差、标准差、RMSE。
- **真实数据应用**：
  - 芝加哥Divvy共享单车系统2016年数据，选择三个代表站点（住宅区Ashland & Wrightwood、市中心Lasalle & Washington、旅游区Shedd Aquarium）。
  - 拟合**趋势+季节性模型**（\(d=7\)，周周期），趋势用三次多项式，季节均值用四次内结点三次样条。
  - 仅展示结果，未进行定量对比或预测验证。

## 4. 资源与算力

- **未明确提及任何计算资源**（GPU型号、数量、训练时长等）。论文仅注明模拟和数据分析使用标准统计计算方法，未说明具体硬件或集群。

## 5. 实验数量与充分性

- **模拟实验数量**：
  - 3种模型 × 3种样本量 = 9种场景，每种1000次重复，共9000次模拟运行。
  - 评估了偏差、标准差、RMSE，结果以表格呈现（表1），显示一致性。
- **真实数据**：3个站点案例，均使用同一模型框架，展示了趋势和季节强度图。
- **充分性评价**：
  - 模拟实验覆盖了理想情形（独立Y）和相关情形（自相关Y），但仅验证趋势模型，**未模拟季节性模型**。
  - **未与任何现有方法进行对比**（如功能数据方法、独立点过程方法等），因此无法判断相对优劣。
  - 真实数据部分**缺乏量化指标**（如拟合优度、预测误差），仅依赖图形展示。
  - 总体而言，实验设计较为基础，未能全面客观验证方法优越性。

## 6. 论文的主要结论与发现

- **理论结论**：提出的M估计量在弱假设下具有**一致性**和**渐近正态性**（定理1,2, 推论3,4）。
- **模拟结论**：有限样本下估计量偏差较小、标准差随样本量增大而减小，**三种模型下均表现一致**，支持理论结果。
- **实际数据发现**：
  - 三个站点的指数化趋势均呈“夏季高冬季低”的年度模式，但增幅差异大（住宅区约9倍，市中心约3倍，旅游区约30倍）。
  - 季节强度曲线反映不同功能区的使用模式：住宅区**双峰**（早晚上下班），市中心**晚高峰**（傍晚下班），旅游区**午后单峰**（周末更高）。
  - 方法成功识别了有明显实际意义的趋势和季节成分。

## 7. 优点

- **方法简洁**：基于样条的工作似然M估计，目标函数凹，优化容易；无需复杂的MCMC或变分推断。
- **理论扎实**：给出了弱假设下的渐近性证明（允许非独立Y_t），并给出“sandwich”方差估计便于构建置信带。
- **灵活性强**：既适用于时间点过程也适用于空间点过程（通过张量积样条）；趋势基底不限定于多项式，可扩展。
- **实际问题驱动**：以共享单车需求为案例，结果具有解释性和应用价值，展示了方法对不同模式站点的区分能力。

## 8. 不足与局限

- **实验覆盖不足**：仅模拟趋势模型，未模拟包含季节性的模型；未与现有方法（如功能主成分分析+平滑、独立点过程模型）进行对比，难以验证方法优势。
- **模型假设限制**：
  - 趋势周期 \(r\) 需为季节周期 \(d\) 的整数倍（A3），实际数据可能不严格满足。
  - 渐近正态性证明假设 \(Y_t\) 独立（A6），虽可放宽但未给出具体操作；实际可能对序列相关敏感。
- **真实数据验证薄弱**：仅展示图形，缺乏数值评价（如拟合优度、预测能力）；未考虑站点间空间相关性等扩展。
- **计算资源未说明**：对于大规模空间点过程或超长期时间序列，计算复杂度未经讨论；论文未提及计算效率或扩展性。
- **潜在偏差风险**：样本量（n=365天）有限，估计的渐近分布可能不准确；未讨论季节长度d的选取（固定为7），对非整周期或夏令时等可能不稳健。

（完）
