---
title: "PHGNet: Prototype-Guided Hypergraph Construction for Heterogeneous Spatiotemporal Forecasting"
title_zh: PHGNet：面向异构时空预测的原型引导超图构建
authors: "Ruiwen Gu, Yahao Liu, Zhenyu Liu, Qitai Tan, Xiao-Ping Zhang"
date: 2026-05-25
pdf: "https://arxiv.org/pdf/2605.25554v1"
tags: ["query:tpp-es"]
score: 8.0
evidence: 利用超图构建进行时空预测，直接针对点过程预测
tldr: 交通预测依赖复杂时空依赖建模，现有方法限于成对交互，难以捕捉动态高阶关系。提出PHGNet，利用原型学习机制将模式相似节点自适应分配到超边，构建时变超图以捕捉高阶交互。通过全局-局部节点表示模块提取时一致特征，结合迭代残差细化与时间查询注意力实现高效并行解码。在多个真实数据集上，PHGNet优于现有方法。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.25554v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 889, \"height\": 327, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.25554v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1504, \"height\": 1014, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.25554v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 887, \"height\": 605, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.25554v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1832, \"height\": 647, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.25554v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 763, \"height\": 194, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.25554v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1842, \"height\": 618, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.25554v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 777, \"height\": 325, \"label\": \"Table\"}]"
motivation: 现有交通预测方法局限于成对空间依赖，难以建模节点间动态高阶交互，导致预测精度受限。
method: 设计原型学习机制自适应构建超图，结合全局-局部节点表示提取时一致特征，并通过迭代残差细化和时间查询注意力实现高效并行预测。
result: 在多个真实交通数据集上，PHGNet相比现有方法取得最优预测性能。
conclusion: PHGNet通过原型引导超图构建有效捕捉动态高阶时空依赖，显著提升预测精度。
---

## 摘要
作为智能交通系统的核心任务，交通预测在城市交通管理中扮演着关键角色。准确的交通预测依赖于对复杂时空依赖关系的建模，而交通系统中的空间异质性使其本身具有挑战性。尽管取得了显著进展，大多数现有方法仍局限于成对空间依赖建模，难以捕捉具有相似交通模式的节点之间的动态高阶交互。为解决此问题，我们提出PHGNet，一种基于原型引导超图构建的新型时空预测框架。在PHGNet的核心，设计了一种原型学习机制，自适应地将模式相似节点分配到超边，从而捕捉具有时变结构的高阶交互。为提高动态超图构建的可靠性，我们进一步开发了全局-局部节点表示模块以提取时间一致性特征。在预测方面，引入了迭代残差优化和时序查询注意力，以提高预测精度并支持高效并行解码。在多个真实世界数据集上的大量实验表明，与最先进方法相比，PHGNet实现了优越的预测性能。

## Abstract
As a core task in intelligent transportation systems, traffic forecasting plays a critical role in urban traffic management. Accurate traffic forecasting relies on modeling complex spatiotemporal dependencies, which is inherently challenging due to spatial heterogeneity in traffic systems.Despite significant progress, most existing methods are still limited to pairwise spatial dependency modeling, making it difficult to capture dynamic high-order interactions among nodes with similar traffic patterns. To address this issue, we propose PHGNet, a novel spatiotemporal forecasting framework based on prototype-guided hypergraph construction. At the core of PHGNet, a prototype learning mechanism is designed to adaptively assign pattern-similar nodes to hyperedges, thereby capturing high-order interactions with time-varying structures. To improve the reliability of dynamic hypergraph construction, we further develop a global-local node representation module to extract time-consistent features. For forecasting, iterative residual refinement and Temporal Query Attention are introduced to improve forecasting accuracy while supporting efficient parallel decoding. Extensive experiments on multiple real-world datasets demonstrate that PHGNet achieves superior predictive performance compared with state-of-the-art methods.

---

## 论文详细总结（自动生成）

# 论文结构化总结

## 1. 核心问题与整体含义（研究动机和背景）

- **核心问题**：交通预测中普遍存在的**空间异质性**（spatial heterogeneity）导致地理相邻但功能角色不同（如住宅区 vs 商业区）的节点具有不同的交通动态。现有方法通常依赖成对关系（pairwise）建模空间依赖，如距离图或可学习图，但无法捕捉节点间基于模式相似性的**高阶交互**（high-order interactions）——即具有相似交通模式的节点可能以群体方式演化，形成动态的、随时间变化的区域级依赖。
- **整体含义**：现有方法在应对空间异质性时存在两大局限：① 成对关系难以描述区域级的高阶关联；② 图结构通常是静态的，无法捕捉时空相关性的动态变化。为解决此问题，论文提出**原型引导超图网络（PHGNet）**，通过动态构建超图来建模高阶、时变的空间依赖，从而提升预测精度。

## 2. 方法论：核心思想、关键技术细节

- **核心思想**：利用**可学习的原型嵌入**（prototype embeddings）作为交通模式表徵，将每个时间步的节点软分配到多个原型上，形成超边（hyperedge），实现从节点到超边的消息传递，再传回节点，从而捕捉高阶模式相似性。同时引入**全局-局部节点表示模块**融合静态空间先验与动态隐状态，保证超图结构的鲁棒性和时变一致性。
- **关键技术细节**：
  1. **时空嵌入模块**：编码时间周期模式（一天中的时段、一周中的星期）和可学习的空间嵌入。
  2. **原型引导超图卷积（Prototype-based HyperGraph Convolution）**：
     - 全局-局部节点表示：通过时间条件门控融合当前隐状态 $H_{t-1}$ 与空间嵌入 $E_s$，得到 $E_{N_t}$。
     - 超图构建：基于 $E_{N_t}$ 与原型库 $P$ 计算软分配矩阵 $S_t = \text{softmax}(E_{N_t} P^T)$，将节点分配给 $M$ 个原型。
     - 超图卷积：分两步——先节点到超边聚合（$H_{e_t} = S_t^T (D_v^{-1/2} X_{E_t}) W_e$），再超边到节点传播（$X_{h_t} = \text{ReLU}(D_v^{-1/2} S_t D_e^{-1} H_{e_t})$），最后将结果与输入拼接并进行节点自适应特征变换。
  3. **编码器-解码器框架**：
     - 基于**超图门控循环单元（HGGRU）**，将标准GRU中的线性变换替换为超图卷积。
     - **时序查询注意力（Temporal Query Attention）**：以未来时间嵌入为查询（Query），历史时间感知表徵为键（Key）和值（Value），实现跨注意力，缓解分布偏移。
     - **并行解码**：直接初始化 $H$ 个解码器状态，通过HGGRU并行生成未来序列。
     - **迭代残差学习**：$L$ 个堆叠块，每块输出一个预测分量 $\hat{Y}_i$ 和一个回测分量 $\hat{X}_i$，原始输入减去回测后传入下一块，最终预测为各块求和。
- **公式与算法流程**（文字说明）：
  - 节点表示融合：$E_{N_t} = \alpha_t W_h H_{t-1} + (1-\alpha_t)E_s$，门控 $\alpha_t$ 由时间嵌入得到。
  - 软分配：$S_t = \text{softmax}(E_{N_t} P^T)$。
  - 超图卷积的两阶段聚合（公式3-4）。
  - 残差学习：$\hat{Y} = \sum_{i=1}^L \hat{Y}_i$，损失为MAE。

## 3. 实验设计

- **数据集**：使用四个真实交通流基准数据集——**PeMS03**（358节点，26202时间步）、**PeMS04**（307节点，16992步）、**PeMS07**（883节点，28224步）、**PeMS08**（170节点，17856步），均来自加州PeMS系统，采样间隔5分钟。
- **Benchmark**：历史12步预测未来12步（输入12个时间片，输出12个时间片），评价指标为MAE、RMSE、MAPE（平均绝对百分比误差）。
- **对比方法**：共10个基线，包括传统方法ARIMA，经典图时空模型STGCN、DCRNN、GWNet、GMAN，动态图方法DGCRN、D2STGNN，以及近期先进模型LASTGNN、DMGSTCN、STDN。

## 4. 资源与算力

- 论文提到实验在**NVIDIA RTX 4090 GPU**上运行，CUDA 12.1。
- **未明确说明**使用的GPU数量、训练时长、总迭代次数等具体算力开销。仅提到batch size默认为64，根据显存可调整为32或16，以及使用了早停策略。

## 5. 实验数量与充分性

- **实验数量**：
  - 主实验：四个数据集上与10个基线对比，结果呈现在表II。
  - 消融实验：在PeMS07和PeMS08上对6个变体（w/o Res、w/o Res+Local、w/o Res+Global、w/o Res+DGC、w/o TE、w/o NAPL）进行对比，表III。
  - 参数敏感性分析：在PeMS04和PeMS08上测试原型数量M（4,6,8,10）和节点嵌入维度DN（不同范围），图3。
  - 可视化：T-SNE投影空间嵌入，展示聚类效果并验证模式相似性。
- **充分性评价**：实验设计较为全面。主实验覆盖多种类型基线（统计、图卷积、动态图、超图）；消融覆盖了残差、全局-局部表示、超图结构、时间嵌入、节点自适应参数等关键组件；参数分析考察了两个超参数；可视化提供了定性验证。整体公平、客观，结果清晰。

## 6. 主要结论与发现

- PHGNet在几乎所有数据集和指标上取得最佳结果，相比最佳基线（D2STGNN等）在MAE、RMSE、MAPE上分别平均降低5.76%、1.50%、4.82%。
- 消融表明：① 残差学习提升了预测精度；② 全局-局部融合优于单独使用局部或全局；③ 超图结构优于自适应图卷积，证明高阶建模有效；④ 时间嵌入和节点自适应参数均贡献显著。
- 参数分析显示适中的原型数量（M=6~8）和合理的嵌入维度能平衡性能与过拟合；规模较小的数据集宜用较低维度。
- 可视化显示空间嵌入形成模式聚类，聚类内节点交通模式相似，异质节点分散，验证了模型捕捉空间异质性的能力。

## 7. 优点

- **创新性**：将原型引导超图应用于交通预测，有效建模动态高阶模式相似性，克服了普通图对空间异质性建模的不足。
- **方法完整性**：结合全局-局部表示、时间条件门控、迭代残差学习、并行解码等多个设计，共同提升鲁棒性和效率。
- **实验严谨性**：在多个大规模真实数据集上验证，与10个主流基线对比，消融实验覆盖主要组件，参数分析提供实用指导。
- **可视化分析**：使用T-SNE展示了空间嵌入的聚类效果，直观证明原型捕捉了语义模式，增强了可解释性。

## 8. 不足与局限

- **实验覆盖局限**：
  - 仅使用交通流量数据（PeMS），未在速度、占用率或其他城市时空数据（如人流、能耗）上验证，泛化能力有待进一步测试。
  - 基线均来自交通领域，未与更广泛的时空预测模型（如基于Transformer的通用时序模型）对比。
- **算力报告不完整**：未提供训练时间、模型参数量、收敛迭代次数等，不利于复现和效率判断。
- **超参数调优的稳健性**：原型数量M和嵌入维度DN在不同数据集上最优值不同（如PeMS04要求较大DN），需要手动调整，可能增加应用成本。
- **评估指标单一**：仅报告宏观平均结果（未来12步的平均值），未展示不同预测步长的性能变化或置信区间，无法判断模型长时预测的衰减速度。
- **潜在偏差**：空间异质性的定义基于交通模式，但未定量分析不同节点的异质性程度对模型的影响；可视化仅展示部分节点，可能存在选择性偏差。

（完）
