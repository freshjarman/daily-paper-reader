---
title: Modeling non-Poissonian temporal hypergraphs by Markovian node dynamics
title_zh: 通过马尔可夫节点动态建模非泊松时序超图
authors: "Hang-Hyun Jo, Naoki Masuda"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.07694v1"
tags: ["query:tpp-es"]
score: 9.0
evidence: 建模时序超图中的突发事件序列和事件间隔时间分布
tldr: 时序超图能描述节点间随时间变化的群体交互。实证数据表明这类交互具有突发性和复杂的时间相关性。本研究提出了一种节点驱动的时序超图模型：每个节点在低活动和高活动状态间随机切换；一个超边产生带时间戳事件的概率取决于其内处于高状态的节点数量。通过两种事件生成规则，我们解析推导了超边和节点的事件序列间隔时间分布与自相关函数。研究发现，尽管节点状态动态是马尔可夫过程，但诱发的事件过程却成为泊松短尾分量的混合体，从而产生了更长尾的间隔时间分布和缓慢衰减的自相关。理论进一步揭示了这些特征对超边大小的依赖性，这与多种实证数据基本一致。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.07694v1/fig-001.webp\", \"caption\": \"FIG. 1. Schematic of the dynamics of six nodes, v1, . . . , v6, and two hyperedges, e1 and e2. The two hyperedges share one node, v3. Variable Xi(t) for i ∈ {1, . . . , 6} denotes the state of vi at time t, which is either h or ℓ. Variable λe,j(t) for j ∈ {1, 2} represents the event probability on the jth hyperedge at time t [Eqs. (1) and (2) for AND and LIN rules, respectively]. Note that 0 < λℓ ≤ λe,j ≤ λh. Variables e1(t), e2(t), and v3(t) represent the time series of events on two hyperedges and node v3; at any t, they are equal to 1 when there is an event and 0 when there is no event. Equation (21) determines v3(t) from e1(t) and e2(t).\", \"page\": 3, \"index\": 1, \"width\": 1064, \"height\": 418}, {\"url\": \"assets/figures/arxiv/2604.07694v1/fig-002.webp\", \"caption\": \"TABLE I. Descriptive statistics of the six empirical temporal hypergraphs. We remind that N is the number of nodes, E is the number of hyperedges, and T is the number of time points.\", \"page\": 8, \"index\": 2, \"width\": 510, \"height\": 309}, {\"url\": \"assets/figures/arxiv/2604.07694v1/fig-003.webp\", \"caption\": \"FIG. 6. Results of empirical data analysis. We show the average event probability, Ωe, the CV of the IET distribution, and the ACF, Ae(td), in the left, center, and right panels, respectively, each for hyperedges. Each row of the figure corresponds to one empirical temporal hypergraph.\", \"page\": 8, \"index\": 3, \"width\": 507, \"height\": 737}]"
motivation: 实证数据显示，带时间戳的群体交互呈现突发性事件序列和非平凡的时间相关性，需要模型来解释这些现象。
method: 提出了节点驱动的时序超图模型，其中节点在低活动和高活动状态间随机切换，超边的交互事件生成概率取决于其内高状态节点的数量。
result: 理论分析表明，尽管节点状态动态是马尔可夫的，但产生的事件过程成为泊松短尾分量的混合，导致更重尾的间隔时间分布和缓慢衰减的自相关，且特征依赖于超边大小，与多种实证数据相符。
conclusion: 该模型为理解个体活动波动如何影响群体交互的时间模式提供了一个简单且可解释的框架。
---

## 摘要
时序超图捕捉节点间随时间演化的群体交互。实证数据表明，带时间戳的群体交互呈现出突发性事件序列和非平凡的时间相关性。在本研究中，我们引入了节点驱动的时序超图模型，其中每个节点随机地在低活动状态和高活动状态之间切换，而一条超边以一定概率产生带时间戳的事件，该概率取决于超边中处于高状态节点的数量。针对两种事件生成规则，我们解析推导了超边和节点的事件序列的间隔时间分布和自相关函数。尽管节点状态动态是马尔可夫的，但所诱导的事件过程成为泊松、短尾分量的混合，从而产生更长尾的间隔时间分布和缓慢衰减的自相关性。理论进一步揭示了这些特征对超边大小的依赖性，这与多种实证数据基本吻合。我们期望我们的模型能提供一个简单、可解释的框架，用于将个体层面的活动波动与现实群体交互中观察到的时间模式联系起来。

## Abstract
Temporal hypergraphs capture time-resolved group interactions among nodes. Empirical data support that time-stamped group interactions show bursty event sequences and non-trivial temporal correlations. In the present study, we introduce node-driven temporal hypergraph models in which each node stochastically alternates between low- and high-activity states, and a hyperedge produces time-stamped events with a probability that depends on the number of high-state nodes in the hyperedge. For two event-generation rules, we analytically derive interevent time distributions and autocorrelation functions of event sequences, both for hyperedges and nodes. Despite Markovian node-state dynamics, the induced event processes become mixtures of Poissonian, short-tailed components, resulting in longer-tailed interevent time distributions and slowly decaying autocorrelation. The theory further shows the dependence of these features on the size of hyperedge, which largely agrees with various empirical data. We expect our models to provide a simple, interpretable framework for connecting individual-level activity fluctuations to the timing patterns observed in real group interactions.

---

## 论文详细总结（自动生成）

# 论文总结：通过马尔可夫节点动态建模非泊松时序超图

## 1. 核心问题与整体含义
*   **研究动机**：现实世界中的群体交互（如会议、合作发表、药物联用）本质上是高阶的，无法分解为二元关系的简单叠加。时序超图是描述这类交互的有力工具。实证数据表明，这些群体交互事件序列具有**突发性**和**非平凡的时间相关性**（即非泊松特性）。然而，现有模型（如高阶活动驱动模型）要么难以产生这种非泊松模式，要么难以进行解析分析。
*   **研究目标**：本文旨在提出一个既具有解析可处理性，又能生成与实证数据相符的非泊松时间模式（如重尾间隔时间分布、缓慢衰减的自相关）的时序超图模型。其核心思想是将群体交互的时间模式追溯到个体节点活动的内在随机波动。

## 2. 方法论
*   **核心思想**：在静态底层超图上，每个节点的活动状态遵循一个简单的两状态（高`h`/低`ℓ`）马尔可夫过程。超边的事件生成概率取决于其内部处于高状态节点的数量。
*   **关键技术细节**：
    1.  **节点动态**：每个节点独立地以概率 `r_ℓh` 从低态翻转到高态，以概率 `r_hℓ` 从高态翻转到低态。
    2.  **事件生成规则（两种）**：
        *   **AND规则**：仅当超边内所有节点都处于高状态时，事件发生概率为 `λ_h`；否则为 `λ_ℓ` (`λ_ℓ < λ_h`)。
        *   **LIN规则**：事件发生概率是 `λ_h` 和 `λ_ℓ` 的线性组合，权重由高状态节点比例决定：`(m_h/m) * λ_h + (1 - m_h/m) * λ_ℓ`。
    3.  **分析推导**：
        *   在离散时间框架下，利用马尔可夫链和贝叶斯定理，精确推导了单个超边和单个节点的间隔时间分布和自相关函数的表达式。
        *   在假设节点状态转换罕见 (`r_ℓh, r_hℓ ≪ 1`) 的条件下，得到了简洁的近似解。结果显示 IET 分布是多个几何分布（对应不同事件率）的加权混合，ACF是指数衰减函数的混合。

## 3. 实验设计
*   **数据集/场景**：
    *   **模型验证实验**：使用人工生成的时序序列验证理论推导的正确性。参数固定为 `r_ℓh =5e-5`, `r_hℓ=2e-5`, `λ_h=0.01`, `λ_ℓ=0.001`。考察不同超边大小 `m` (2到16)下的平均事件率、IET分布、变异系数和ACF。
    *   **实证数据分析**：使用了6个公开的真实世界时序超图数据集：
        1.  高中接触网络 (High school)
        2.  小学接触网络 (Primary school)
        3.  计算机科学文献合作网络 (DBLP)
        4.  药物滥用警告网络 (DAWN)
        5.  国家药品代码-类别网络 (NDC-class)
        6.  国家药品代码-成分网络 (NDC-substance)
*   **Benchmark与对比方法**：
    *   主要将模型的预测趋势与实证数据的统计特征进行定性比较。
    *   理论上的基准是伯努利过程（离散泊松过程），用于对比证明模型能产生更重尾的IET分布和非平凡的ACF。
    *   文中未与其他复杂计算模型进行直接性能对比。

## 4. 资源与算力
*   论文中未明确提及实验所使用的具体硬件配置（如GPU型号、数量）、软件环境或训练时长。数值模拟部分仅描述了生成了 `10^3`条长度为 `T =10^7`的时间序列用于统计。

## 5. 实验数量与充分性
*   **实验组数**：
    1.  **模型验证实验**：针对两种规则（AND/LIN），系统改变了超边大小(`m`)和节点所属超边数与大小(`k, m`)的组合，进行了多组模拟以验证平均事件率、IET分布、CV和ACF的理论公式。图2-5展示了关键结果。
    2.  **实证分析实验**：对6个不同领域的数据集进行了分析，分别计算了不同大小(`m`)的超边的平均事件率、IET的变异系数(CV)和ACF。
*   **充分性与客观性评估**：
    *   **充分性方面较好之处**: a)对模型本身的理论性质进行了系统的参数扫描和分析；b)使用了多个来源、不同性质的实证数据集进行趋势验证；c)同时分析了超边和节点层面的统计量。
    *   **局限性**: a)主要是定性趋势对比（如CV随m增大而减小），缺乏严格的定量拟合优度检验或参数推断；b)未与其他时序超图生成模型进行头对头的性能比较；c)对节点的分析仅限于简单拓扑结构（如属于两个相同大小超边的节点）。

## 6.主要结论与发现
1.  **理论发现**:尽管驱动模型的节点动态是马尔可夫的（无记忆），但通过AND或LIN规则耦合后产生的群体事件过程却表现出“记忆效应”，其IET分布是多个短尾几何分布的混合，导致整体呈现更重尾的特征，且ACF缓慢衰减。
2.  **尺寸依赖性**: IET分布的分散程度（用CV衡量）以及平均事件率均依赖于超边的大小(`m`) 。在AND规则下，两者都随m增大而减小；在LIN规则下，CV随m增大而减小。这与多数实证数据的观察趋势一致。
3.  **规则比较**:实证数据显示平均事件率和CV通常随m增加而下降，这更符合AND规则的预测趋势而非LIN规则。
4.  **框架价值**:该模型建立了个体活动状态的随机波动与观测到的群体交互复杂时间模式之间的直接联系，提供了一个解析上易于处理的建模框架。

##7 .优点
1.**解析可处理性**:在相对简单的假设下,能够解析推导出关键的时序统计量(IET分布, ACF),深化了对模型行为的理解.
2.**机制直观**:模型核心思想清晰——将群体动态溯源至个体状态的二元涨落,具有很好的可解释性.
3.**普适性**:提出的框架适用于任何静态底层超图,且两种规则涵盖了从“全体一致”(AND )到“比例响应”(LIN )的不同耦合机制.
4.**与实证吻合**:理论预测的超边大小依赖性在多个异构的真实数据集中得到了定性验证,增强了模型的现实意义.

##8 .不足与局限
1.**简化假设较强**:要求节点状态转换速率远小于事件生成速率(`r ≪ λ`)以获得简洁近似解;节点状态动态被限制为相同的二态马尔可夫过程,可能过于简化.
2.**缺乏定量拟合与推断**:工作侧重于展示模型能产生类似实证的趋势,但没有开发从数据中估计模型参数(如 `r_ℓh , λ_h`)的统计方法,限制了其直接应用于实际数据建模的能力.
3.**拓扑结构简单化**:在对节点的理论分析中,通常假设其所属的超边大小相同或结构规整,对于高度异质的真实超图拓扑的推广可能面临挑战.
4.**忽略周期性等复杂模式**:在筛选实证数据时排除了具有强烈日/周周期性的数据集,因为本模型并非为刻画此类周期性而设计.
5.**对比基准有限**:主要与泊松过程比较以凸显非泊松特性,但未与现代更复杂的时序hypergraph生成模型进行全面性能对比.

(完)

## 9. 潜在影响与未来方向
1.  **理论影响**：该工作为理解高阶交互中的时间相关性提供了一个新的、基于微观机制的解析视角。它表明，即使个体动态是简单的马尔可夫过程，通过群体交互规则的耦合，也能涌现出复杂的集体时间模式（如重尾间隔、长程记忆），这深化了对“非泊松动力学”起源的认识。
2.  **方法学影响**：模型框架具有可扩展性。未来的工作可以在此基础上引入更复杂的节点动态（如多状态、非齐次马尔可夫链）、考虑超边之间的相关性或演化，以及探索其他事件生成规则。
3.  **应用前景**：
    *   **预测与异常检测**：一旦参数可以从数据中估计，该模型可用于预测未来群体事件的发生概率或识别异常活跃模式。
    *   **网络推断**：通过分析观测到的事件序列的时间模式，有可能反推底层静态超图的结构或节点状态的隐含动力学。
    *   **干预策略模拟**：在流行病学或信息传播场景中，可以通过调整模型参数（如改变节点状态转换率 `r`）来模拟不同干预措施对群体接触模式的影响。

## 10. 总结评价
本文是一项扎实的理论建模工作。其核心贡献在于构建了一个在解析可处理性与现实描述能力之间取得良好平衡的时序超图模型。通过将群体事件的非泊松特性归因于个体活动状态的随机波动，并用简洁的马尔可夫节点动态与两种交互规则加以实现，作者不仅推导出了关键时间统计量的封闭形式解，还成功地在多个实证数据集上验证了模型所预测的定性趋势（特别是超边大小依赖性）。

尽管存在一些局限性（如假设较强、缺乏参数估计方法），但该模型作为一个“解释性模型”而非“拟合性模型”，成功地阐明了从简单个体行为到复杂群体时间模式的一种可能机制。它为后续开发更复杂、更具拟合能力的时序超图模型奠定了重要的理论基础，并启发了从时间序列中挖掘高阶结构信息的新思路。

（完）
