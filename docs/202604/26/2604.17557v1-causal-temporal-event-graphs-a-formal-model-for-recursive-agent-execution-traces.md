---
title: "Causal-Temporal Event Graphs: A Formal Model for Recursive Agent Execution Traces"
title_zh: 因果-时间事件图：递归智能体执行轨迹的一种形式化模型
authors: Simon Foldvik
date: 2026-04-19
pdf: "https://arxiv.org/pdf/2604.17557v1"
tags: ["query:tpp-es"]
score: 8.0
evidence: 带有时间戳和事件类型的递归代理执行轨迹的形式化模型
tldr: 本研究针对递归智能体执行轨迹的建模难题，提出了因果-时间事件图（CTEG）形式化模型。该模型通过单亲因果语义，将事件发射和子智能体调用定义为通用时序图上的扩展过程。CTEG 采用根状树结构，确保时间戳沿因果路径严格递增，并利用不动点理论证明了递归执行轨迹的收敛性。该框架支持分布式组合构建、容错处理及加密验证，为复杂智能体系统的可追溯性和验证提供了理论基础。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.17557v1/fig-001.webp\", \"caption\": \"Figure 6. Evolution from state Gk to Gk+1 by Ed-subagent invocation.\", \"page\": 11, \"index\": 1, \"width\": 658, \"height\": 364}, {\"url\": \"assets/figures/arxiv/2604.17557v1/fig-002.webp\", \"caption\": \"Figure 1. Recursive E2-agent execution. Timestamps and types are omitted for clarity.\", \"page\": 2, \"index\": 2, \"width\": 717, \"height\": 180}, {\"url\": \"assets/figures/arxiv/2604.17557v1/fig-003.webp\", \"caption\": \"Figure 3. Illustrating the graft of a causal graph G2 into G1.\", \"page\": 7, \"index\": 3, \"width\": 600, \"height\": 287}, {\"url\": \"assets/figures/arxiv/2604.17557v1/fig-004.webp\", \"caption\": \"Figure 5. Evolution from state Gk to Gk+1 by direct emissions.\", \"page\": 10, \"index\": 4, \"width\": 480, \"height\": 260}, {\"url\": \"assets/figures/arxiv/2604.17557v1/fig-005.webp\", \"caption\": \"Figure 2. Global E2execution trace for the agentic computation outlined in Figure 1. Timestamps and types are omitted for clarity.\", \"page\": 4, \"index\": 5, \"width\": 352, \"height\": 459}]"
motivation: 旨在为具有递归调用特征的智能体执行记录建立一套严谨的形式化模型，以解决复杂执行轨迹的追踪与验证问题。
method: 提出了基于根状树结构的因果-时间事件图（CTEG），利用单调算子的克莱尼上升链来定义递归执行的层次结构。
result: 证明了递归闭包由有限的 CTEG 序列组成，并展示了该模型在无需中心化协调的情况下支持组合构建和部分执行失败的恢复。
conclusion: CTEG 模型不仅兼容关系型数据库存储，还支持默克尔树加密承诺，为构建防篡改且可验证的智能体执行系统提供了有效方案。
---

## 摘要
我们引入了因果-时间事件图（CTEGs），作为单亲因果语义下完全解析的递归智能体执行记录的一种形式化模型。我们将直接事件发射和递归子智能体调用形式化为通用类型时间图上的扩展过程，并证明了从单一因果根开始的诱导极大动态的递归闭包 $\mathscr{E}_\infty$ 完全由 CTEGs 的有限序列组成。CTEG 是一种有根树形图（arborescence），其节点带有时间戳和事件类型，并满足时间戳沿因果路径严格递增的约束。我们将 $\mathscr{E}_\infty$ 实现为以递归深度为参数的智能体执行层级递归体系 $\mathscr{E}_0 \subseteq \mathscr{E}_1 \subseteq \cdots$ 的递增并集，该体系被识别为单调算子 $\varphi$ 的升 Kleene 链，且 $\mathscr{E}_\infty$ 为其最小不动点。尽管引入完整的层级体系是自然的，但如果坚持将子智能体执行轨迹的内部构造视为一个委托且不透明的计算单元，那么在 $\mathscr{E}_1$ 处即可实现稳定。CTEG 形式化方法支持在无需中心化协调的情况下，从局部智能体行为组合构建全局良构的执行轨迹，在部分执行失败时仍能保持良构性，并支持自然的关系型数据库编码。此外，CTEGs 的树形结构与加密 Merkle 树承诺兼容，可用于防篡改的会话验证。

## Abstract
We introduce causal-temporal event graphs (CTEGs) as a formal model for fully resolved recursive agent execution records under single-parenthood causal semantics. We formalise direct event emissions and recursive subagent invocations as extension procedures on generic typed temporal graphs and show that the recursive closure $\mathscr{E}_\infty$ of the induced maximal dynamics starting from single causal roots consists entirely of finite sequences of CTEGs. A CTEG is a rooted arborescence whose nodes carry timestamps and event types, subject to the constraint that timestamps be strictly increasing along causal paths. We realise $\mathscr{E}_\infty$ as the increasing union of a recursive hierarchy $\mathscr{E}_0 \subseteq \mathscr{E}_1 \subseteq \cdots$ of agent execution levels parametrised by recursion depth, which is recognised as the ascending Kleene chain of a monotone operator $\varphi$ admitting $\mathscr{E}_\infty$ as its least fixed point. Although the introduction of the full hierarchy is natural, stabilisation occurs already at $\mathscr{E}_1$ if one insists that the internal construction of a subagent execution trace be a delegated and opaque computational unit. The CTEG formalism supports compositional construction of globally well-formed execution traces from local agent behaviour without centralised coordination, preserves well-formedness under partial execution failure, and admits a natural relational database encoding. The arborescent structure of CTEGs is further compatible with cryptographic Merkle tree commitments for tamper-evident session verification.

---

## 论文详细总结（自动生成）

这是一份关于论文《Causal-Temporal Event Graphs: A Formal Model for Recursive Agent Execution Traces》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）驱动的智能体（Agent）工作流日益普及，其执行过程的**可解释性、审计性与合规性**变得至关重要。传统的线性日志（Linear Traces）虽然能记录事件的时间顺序，但难以捕捉复杂的**递归调用关系**和**因果逻辑**（例如，一个工具的输出是由哪个具体的调用引起的）。
*   **核心问题**：如何为具有递归调用特征的智能体执行轨迹建立一个严谨的数学模型，以确保在分布式、异步且可能发生部分失败的环境下，依然能构建出全局一致、逻辑完备的执行记录。
*   **研究背景**：现有的工程实践缺乏对递归智能体因果结构的形式化处理，该论文旨在填补这一理论空白。

### 2. 方法论：核心思想与关键技术
论文提出了**因果-时间事件图（CTEG）**模型，其核心思想是将执行轨迹视为一种特殊的图结构：
*   **CTEG 定义**：一种有根树形图（Arborescence），节点包含时间戳和事件类型。其核心约束是**时间戳沿因果路径严格递增**（即原因必须先于结果）。
*   **局部动态操作**：
    *   **直接发射（Direct Emission）**：智能体在当前节点下产生新的子节点。
    *   **子智能体调用（Subagent Invocation）**：父智能体调用子智能体，子智能体完成其内部执行后，父智能体将其整个执行轨迹“嫁接”（Grafting）到调用点。
*   **递归层级体系**：通过定义单调算子 $\varphi$ 和 Kleene 升链，构建了 $\mathscr{E}_0 \subseteq \mathscr{E}_1 \subseteq \cdots$ 的层级。
    *   $\mathscr{E}_0$ 仅包含直接发射。
    *   $\mathscr{E}_{d+1}$ 允许调用深度为 $d$ 的子智能体。
*   **不动点理论**：证明了所有可能的执行序列最终收敛于最小不动点 $\mathscr{E}_\infty$，且证明了即使在 $\mathscr{E}_1$ 层级，由于子智能体接口的不透明性，系统已经趋于稳定（Stabilisation）。

### 3. 实验设计与 Benchmark
*   **性质说明**：本论文属于**理论计算科学与逻辑**领域，侧重于数学证明和形式化建模，而非实证驱动的实验论文。
*   **对比对象**：论文在逻辑层面对比了“线性轨迹模型”与“CTEG 模型”，通过 Remark 2.13 举例说明了仅靠时间顺序无法唯一还原因果结构，从而论证了 CTEG 的必要性。
*   **验证方式**：通过数学定理（如 Theorem 3.14 和 3.16）证明了该模型在递归闭包下的良构性（Well-formedness）和稳定性。

### 4. 资源与算力
*   **算力消耗**：文中**未提及**任何 GPU、算力或训练时长。
*   **原因**：该研究是纯理论推导和形式化定义，不涉及深度学习模型的训练或大规模数据处理。

### 5. 实验数量与充分性
*   **理论充分性**：论文提供了完整的数学证明过程，涵盖了从基础定义到递归闭包的收敛性分析。
*   **实证充分性**：从工程角度看，缺乏在真实 Agent 框架（如 LangGraph 或 AutoGPT）中的大规模部署测试。但作为形式化模型，其逻辑自洽性已通过数学手段得到验证。

### 6. 主要结论与发现
*   **全局良构性**：局部智能体的正确行为可以组合成全局正确的执行轨迹，无需中心化协调。
*   **层级稳定性**：证明了递归执行层级在 $\mathscr{E}_1$ 处即稳定，这意味着父智能体只需将子智能体视为“黑盒”即可维护完整的因果链。
*   **容错性**：即使执行中途失败，已生成的局部轨迹依然符合 CTEG 定义，支持部分序列的序列化与存储。
*   **可验证性**：CTEG 的树形结构天然适配 **Merkle Tree（默克尔树）**，可实现防篡改的加密验证。

### 7. 优点
*   **严谨性**：为模糊的智能体日志记录提供了坚实的数学基础。
*   **组合性**：支持分布式构建，每个智能体只需负责自身的局部轨迹，降低了系统耦合度。
*   **实用性**：模型设计考虑了关系型数据库的存储需求（父节点指针结构）和现代安全需求（加密承诺）。

### 8. 不足与局限
*   **语义限制**：目前仅支持“单亲因果语义”（每个事件只有一个直接诱因），无法处理复杂的“多对多”因果网络。
*   **交互限制**：未深入探讨智能体之间除父子调用外的直接通信（如对等协作）。
*   **抽象性**：论文未提供具体的代码实现库或针对特定 LLM 幻觉导致的错误因果归因的检测机制。
*   **时间戳依赖**：模型高度依赖系统时钟的准确性，在极端并发情况下可能需要额外的破缺机制（Tie-breaking）。

（完）
