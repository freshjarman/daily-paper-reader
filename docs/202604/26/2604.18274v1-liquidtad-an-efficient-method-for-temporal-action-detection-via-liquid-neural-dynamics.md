---
title: "LiquidTAD: An Efficient Method for Temporal Action Detection via Liquid Neural Dynamics"
title_zh: LiquidTAD：一种基于液体神经动力学的高效时序动作检测方法
authors: "Zepeng Sun, Naichuan Zheng, Hailun Xia, Junjie Wu, Liwei Bao, Xiaotai Zhang"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.18274v1"
tags: ["query:tpp-es"]
score: 8.0
evidence: 用于时间动作检测的液态神经网络中的连续时间动力学
tldr: 针对时序动作检测（TAD）中Transformer架构计算复杂度高且参数冗余的问题，本文提出LiquidTAD框架。该方法首次将液体神经网络（LNN）引入TAD领域，通过闭式连续时间（CfC）公式实现ActionLiquid模块的并行化，具备O(N)线性复杂度。LiquidTAD利用可学习的时间常数自适应调节时序敏感度，在大幅减少参数量的同时保持了高性能，为资源受限环境下的动作检测提供了高效方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.18274v1/fig-001.webp\", \"caption\": \"TABLE V: Ablation results for (a) temporal pyramid levels and (b) discretization step ∆t on THUMOS-14. Default settings are in bold.\", \"page\": 4, \"index\": 1, \"width\": 516, \"height\": 354}, {\"url\": \"assets/figures/arxiv/2604.18274v1/fig-002.webp\", \"caption\": \"TABLE III: Complexity and performance analysis across THUMOS-14, ActivityNet-1.3, and Ego4D datasets.\", \"page\": 3, \"index\": 2, \"width\": 516, \"height\": 257}, {\"url\": \"assets/figures/arxiv/2604.18274v1/fig-003.webp\", \"caption\": \"TABLE I: State-of-the-art comparison on the THUMOS-14 and ActivityNet-1.3 datasets. We report mAP (%) at various tIoU thresholds and the average mAP. The benchmarks are grouped by their respective backbones and architectures.\", \"page\": 3, \"index\": 3, \"width\": 1042, \"height\": 321}]"
motivation: 现有的Transformer架构在处理长视频动作检测时存在计算复杂度高和参数冗余的问题，限制了其在资源受限环境下的应用。
method: 提出基于闭式连续时间（CfC）公式的并行化ActionLiquid模块，利用线性复杂度的液体神经动力学捕捉复杂的时序依赖。
result: "在THUMOS-14数据集上达到69.46%的平均mAP，且参数量仅为10.82M，较ActionFormer基准减少了63%。"
conclusion: LiquidTAD在多个基准测试中实现了最优的精度与效率平衡，并对时序采样变化表现出极强的鲁棒性。
---

## 摘要
未剪辑视频中的时序动作检测（TAD）目前由基于 Transformer 的架构主导。尽管性能优异，但其二次方计算复杂度和大量的参数冗余限制了其在资源受限环境中的部署。在本文中，我们提出了 LiquidTAD，这是一种新型的参数高效框架，它使用并行化的 ActionLiquid 模块取代了繁琐的自注意力层。与受限于串行执行瓶颈的传统液体神经网络（LNNs）不同，LiquidTAD 利用闭式连续时间（CfC）公式，使模型能够重新表述为可并行化的算子，同时保留了连续时间动力学的内在物理先验。该架构以 $O(N)$ 的线性复杂度捕捉复杂的时序依赖关系，并通过学习到的时间常数（$τ$）自适应地调节时序敏感性，为处理不同持续时间的动作提供了一种鲁棒机制。据我们所知，这项工作首次将基于并行化 LNN 的架构引入 TAD 领域。在 THUMOS-14 数据集上的实验结果表明，LiquidTAD 仅需 10.82M 参数即可实现 69.46% 的极具竞争力的平均 mAP，与 ActionFormer 基准相比参数量减少了 63%。在 ActivityNet-1.3 和 Ego4D 基准上的进一步评估证实，LiquidTAD 实现了最佳的准确率-效率权衡，并对时序采样变化表现出卓越的鲁棒性，推进了现代 TAD 框架的帕累托前沿。

## Abstract
Temporal Action Detection (TAD) in untrimmed videos is currently dominated by Transformer-based architectures. While high-performing, their quadratic computational complexity and substantial parameter redundancy limit deployment in resource-constrained environments. In this paper, we propose LiquidTAD, a novel parameter-efficient framework that replaces cumbersome self-attention layers with parallelized ActionLiquid blocks. Unlike traditional Liquid Neural Networks (LNNs) that suffer from sequential execution bottlenecks, LiquidTAD leverages a closed-form continuous-time (CfC) formulation, allowing the model to be reformulated as a parallelizable operator while preserving the intrinsic physical prior of continuous-time dynamics. This architecture captures complex temporal dependencies with $O(N)$ linear complexity and adaptively modulates temporal sensitivity through learned time-constants ($τ$), providing a robust mechanism for handling varying action durations. To the best of our knowledge, this work is the first to introduce a parallelized LNN-based architecture to the TAD domain. Experimental results on the THUMOS-14 dataset demonstrate that LiquidTAD achieves a highly competitive Average mAP of 69.46\% with only 10.82M parameters -- a 63\% reduction compared to the ActionFormer baseline. Further evaluations on ActivityNet-1.3 and Ego4D benchmarks confirm that LiquidTAD achieves an optimal accuracy-efficiency trade-off and exhibits superior robustness to temporal sampling variations, advancing the Pareto frontier of modern TAD frameworks.

---

## 论文详细总结（自动生成）

以下是对论文《LiquidTAD: An Efficient Method for Temporal Action Detection via Liquid Neural Dynamics》的结构化深入总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
时序动作检测（TAD）旨在从未剪辑的视频中精确定位动作的起始时间并识别类别。当前该领域由基于 Transformer 的架构主导，虽然性能卓越，但存在两个核心痛点：
*   **计算复杂度高**：自注意力机制具有二次方复杂度 $O(T^2)$，处理长视频时计算量激增。
*   **参数冗余严重**：为了捕捉多尺度时序上下文，通常需要堆叠大量宽大的前馈网络（FFN）和投影矩阵，导致模型参数量巨大（常超过 30M），难以在资源受限的嵌入式设备上部署。
**研究动机**：探索一种能够替代 Transformer 且具备线性复杂度、参数高效且能捕捉复杂连续动力学的模型。

### 2. 论文提出的方法论
LiquidTAD 首次将**液体神经网络（LNN）**引入 TAD 领域，其核心思想是将时序建模视为一个连续时间动力系统。

*   **核心模块：ActionLiquid Block**
    *   **并行化设计**：不同于传统 LNN 需要串行求解 ODE，该方法采用**闭式连续时间（CfC）**公式，将其重新表述为可并行计算的算子，解决了训练效率问题。
    *   **更新公式**：$x_{out} = x \odot e^{-\tau \Delta t} + M(\tilde{x}) \odot (1 - e^{-\tau \Delta t}) \odot G(\tilde{x}) \odot M$。其中 $e^{-\tau \Delta t}$ 是保留因子（历史信息），$(1 - e^{-\tau \Delta t})$ 是创新率（新信息）。
    *   **可学习的时间常数 ($\tau$)**：模型为每个模块学习一个标量 $\tau$，用于自适应调节对不同时长动作的敏感度。
*   **分层架构**：在特征金字塔（FPN）的每一层部署轻量级的 ActionLiquid 块。低层学习高频信号（短动作），高层捕捉长程依赖（长动作）。
*   **线性复杂度**：计算复杂度为 $O(CkT)$（$C$ 为通道，$k$ 为卷积核大小，$T$ 为序列长度），实现了严格的线性增长。

### 3. 实验设计
*   **数据集**：
    *   **THUMOS-14**：侧重于密集且短促的动作定位。
    *   **ActivityNet-1.3**：侧重于长持续时间的视频。
    *   **Ego4D (Moment Queries)**：第一人称视角视频，背景运动复杂。
*   **Benchmark 与对比方法**：
    *   对比了包括 **ActionFormer** (Transformer 基准)、**TriDet**、**CausalTAD**、**TadTR**、**DiffTAD** 等在内的多种 SOTA 模型。
    *   使用了 I3D、TSP 和 InternVideo 等主流预训练特征提取器。

### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的 GPU 型号、数量以及具体的训练总时长。
*   **实现细节**：提到基于 `OpenTAD` 代码库实现，使用 `AdamW` 优化器，物理时间步长 $\Delta t$ 与特征提取步长对齐。

### 5. 实验数量与充分性
*   **实验规模**：论文在三个不同性质的大规模数据集上进行了验证，涵盖了从短动作到长动作、从第三人称到第一人称视角的场景。
*   **消融实验**：
    1.  **$\tau$ 学习策略**：对比了逐通道学习与共享标量学习，证明共享标量能提供更强的归纳偏置。
    2.  **金字塔深度**：测试了 4 到 8 层 FPN 的影响。
    3.  **$\Delta t$ 鲁棒性**：测试了推理时改变采样频率对性能的影响。
*   **充分性评价**：实验设计较为全面，通过对比参数量（Params）和计算量（FLOPs）直观展示了效率优势，实验结果客观且具有说服力。

### 6. 论文的主要结论与发现
*   **极高的参数效率**：在 THUMOS-14 上，LiquidTAD 仅用 **10.82M** 参数就达到了 69.46% 的 mAP，比 ActionFormer **减少了 63% 的参数**。
*   **性能领先**：在 ActivityNet-1.3 上达到 37.06% 的平均 mAP，超越了许多重型模型。
*   **数值弹性（Numerical Elasticity）**：发现学习到的 $\tau$ 能够补偿时间步长 $\Delta t$ 的变化。即使推理时的采样频率与训练时不一致，模型依然能保持稳定的性能，这是离散 Transformer 模型不具备的特性。

### 7. 优点（亮点）
*   **首创性**：首次将液体神经网络引入 TAD 任务，并成功实现了并行化。
*   **帕累托前沿推进**：在“精度-效率”权衡曲线上表现极佳，非常适合移动端或实时视频分析。
*   **物理先验**：利用连续时间动力学公式，使模型对动作持续时间的建模更具解释性和鲁棒性。
*   **线性扩展性**：解决了 Transformer 处理超长视频时的内存爆炸问题。

### 8. 不足与局限
*   **绝对精度上限**：虽然效率极高，但在某些数据集（如 Ego4D）上，其绝对 mAP 相比于参数量巨大的模型（如 CausalTAD，76M 参数）仍有一定差距。
*   **超参数敏感性**：虽然 $\tau$ 是自学习的，但初始 $\tau$ 的设置或 $\Delta t$ 的定义可能对不同特征提取器的适配性存在潜在影响。
*   **硬件优化**：虽然理论复杂度低，但指数运算（$e^{-\tau \Delta t}$）在某些特定硬件上的底层优化可能不如标准卷积或矩阵乘法成熟。

（完）
