---
title: Windowed thinning and query complexity for the bouncy particle and Zigzag samplers
title_zh: 弹跳粒子和Zigzag采样器的窗口细化与查询复杂度
authors: "Jianfeng Lu, Yinchen Luo"
date: 2026-07-30
pdf: "https://arxiv.org/pdf/2607.28413v1"
tags: ["query:tpp-es"]
score: 6.0
evidence: 窗口抽稀用于连续时间事件率的精确模拟，可直接迁移至时间点过程仿真。
tldr: "针对R^d上势能U满足m-强凸与L-光滑的目标分布μ(dx)∝e^{-U(x)}dx，条件数为κ=L/m，研究弹跳粒子采样器与坐标Zigzag过程的精确模拟。提出窗口化减薄方法：将轨迹分成确定性窗口，在每窗口起点用一次梯度评估构造事件率的局部包络，从而高效决定反弹或翻转。结合定量混合估计与反弹/翻转次数的有限时间界，从高斯冷启动得到总变差误差ε下的查询复杂度：BPS为O(κ^{1/2}d(d logκ+log(1/ε)))次梯度查询，Zigzag为O(κ d^{1/4}(d logκ+log(1/ε)))次全梯度等价（d个坐标偏导计为一次）。"
source: arxiv
selection_source: fresh_fetch
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.28413v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1172, \"height\": 296, \"label\": \"Table\"}]"
motivation: 弹跳粒子采样器和Zigzag过程的精确模拟需处理复杂事件率，现有方法查询成本高，缺乏从高斯冷启动的复杂度保证。
method: 将轨迹划分为确定性窗口，在每个窗口起点评估一次梯度构造事件率局部包络，并利用泊松过程精确模拟反弹或翻转。
result: "在m-强凸和L-光滑条件下，从高斯冷启动达到总变差误差ε，BPS需O(κ^{1/2}d(d logκ+log(1/ε)))梯度查询，Zigzag需O(κ d^{1/4}(d logκ+log(1/ε)))全梯度等价。"
conclusion: 首次给出窗口化减薄下两种采样器的查询复杂度保证，为高维对称分布采样提供了理论依据。
---

## 摘要
设$\mu(d x)\propto e^{-U(x)} d x$定义在$\mathbb{R}^d$上，其中$U$是$m$-强凸且$L$-光滑的，并记条件数为$\kappa=L/m$。我们考虑窗口细化，这是一种用于弹跳粒子采样器和坐标Zigzag过程的精确模拟方法。该方法将轨迹划分为确定性窗口，并在每个窗口开始时使用一次梯度评估来构造事件率的可处理的局部包络。将该构造与定量混合估计以及预期弹跳次数和翻转次数的有限时间界相结合，可以从高斯冷启动获得查询复杂度保证。对于全变差误差$\varepsilon$，弹跳粒子采样器的预期查询次数为$O(\kappa^{1/2}d\,(d\log\kappa+\log\frac1\varepsilon))$次梯度查询，而Zigzag过程的预期查询次数为$O(\kappa d^{1/4}(d\log\kappa+\log\frac1\varepsilon))$次全梯度等价，其中$d$个坐标部分查询计为一个等价查询。

## Abstract
Let $μ(d x)\propto e^{-U(x)} d x$ on $\R^d$, where $U$ is $m$-strongly convex and $L$-smooth, and denote by $κ=L/m$ the condition number. We consider windowed thinning, an exact simulation method for the bouncy particle sampler and the coordinate Zigzag process. The method divides a trajectory into deterministic windows and uses a gradient evaluation at the beginning of each window to construct a tractable local envelope for the event rate. Combining this construction with quantitative mixing estimates and finite-time bounds on the expected numbers of bounces and flips yields query complexity guarantees from a Gaussian cold start. For total-variation error $\varepsilon$, the expected query counts are $O(κ^{1/2}d\,(d\logκ+\log\frac1\varepsilon))$ gradient queries for the bouncy particle sampler and $O(κd^{1/4}(d\logκ+\log\frac1\varepsilon))$ full-gradient equivalents for Zigzag, where $d$ coordinate-partial queries count as one equivalent.