---
title: "TIGFlow-GRPO: Trajectory Forecasting via Interaction-Aware Flow Matching and Reward-Driven Optimization"
title_zh: TIGFlow-GRPO：基于交互感知流匹配与奖励驱动优化的轨迹预测
authors: "Xuepeng Jing, Wenhuan Lu, Hao Meng, Zhizhi Yu, Jianguo Wei"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.24936v1"
tags: ["query:tpp-es"]
score: 8.0
evidence: 从时空观测中建模轨迹分布
tldr: 针对自动驾驶和人群监控中的轨迹预测问题，本文提出 TIGFlow-GRPO 框架。该框架结合了条件流匹配（CFM）与轨迹交互图（TIG）来捕捉复杂的时空交互，并引入基于奖励驱动的 GRPO 优化阶段。通过将确定性流推导转化为随机 SDE 采样，并结合社会规范与地图约束的复合奖励进行后训练，显著提升了预测轨迹的社会合规性和物理可行性。
selection_source: fresh_fetch
motivation: 现有的流匹配轨迹预测方法主要依赖监督学习，难以充分反映复杂的社会规范和场景物理约束。
method: 提出两阶段框架，首先利用轨迹交互图增强上下文编码，随后通过随机微分方程采样与组相对策略优化（GRPO）进行行为对齐。
result: 在 ETH/UCY 和 SDD 数据集上的实验表明，该方法在提高预测准确性的同时，增强了长时稳定性和环境适应性。
conclusion: 该研究成功将流匹配生成模型与强化学习对齐技术相结合，为动态环境下的行为感知轨迹建模提供了有效方案。
---

## 摘要
人类轨迹预测对于在视觉复杂环境中运行的智能多媒体系统（如自动驾驶和人群监控）至关重要。尽管条件流匹配（CFM）在根据时空观测建模轨迹分布方面表现出强大的能力，但现有方法仍主要侧重于监督拟合，这可能导致生成的轨迹无法充分反映社交规范和场景约束。为了解决这一问题，我们提出了 TIGFlow-GRPO，这是一个将基于流的轨迹生成与行为规则对齐的两阶段生成框架。在第一阶段，我们构建了一个带有轨迹交互图（TIG）模块的 CFM 预测器，以建模细粒度的视觉空间交互并增强上下文编码。该阶段更有效地捕捉了智能体与智能体以及智能体与场景之间的关系，为后续对齐提供了更具信息量的条件特征。在第二阶段，我们进行 Flow-GRPO 后训练，其中确定性流展开被重新表述为随机的 ODE 到 SDE 采样以实现轨迹探索，并且复合奖励结合了视图感知的社交合规性与地图感知的物理可行性。通过评估 SDE 展开探索的轨迹，GRPO 逐步引导多模态预测走向行为合理的未来。在 ETH/UCY 和 SDD 数据集上的实验表明，TIGFlow-GRPO 在提高预测准确性和长时稳定性的同时，生成的轨迹更符合社交规范且具有物理可行性。这些结果表明，所提出的框架为在动态多媒体环境中将基于流的轨迹建模与行为感知对齐相结合提供了一种有效途径。

## Abstract
Human trajectory forecasting is important for intelligent multimedia systems operating in visually complex environments, such as autonomous driving and crowd surveillance. Although Conditional Flow Matching (CFM) has shown strong ability in modeling trajectory distributions from spatio-temporal observations, existing approaches still focus primarily on supervised fitting, which may leave social norms and scene constraints insufficiently reflected in generated trajectories. To address this issue, we propose TIGFlow-GRPO, a two-stage generative framework that aligns flow-based trajectory generation with behavioral rules. In the first stage, we build a CFM-based predictor with a Trajectory-Interaction-Graph (TIG) module to model fine-grained visual-spatial interactions and strengthen context encoding. This stage captures both agent-agent and agent-scene relations more effectively, providing more informative conditional features for subsequent alignment. In the second stage, we perform Flow-GRPO post-training,where deterministic flow rollout is reformulated as stochastic ODE-to-SDE sampling to enable trajectory exploration, and a composite reward combines view-aware social compliance with map-aware physical feasibility. By evaluating trajectories explored through SDE rollout, GRPO progressively steers multimodal predictions toward behaviorally plausible futures. Experiments on the ETH/UCY and SDD datasets show that TIGFlow-GRPO improves forecasting accuracy and long-horizon stability while generating trajectories that are more socially compliant and physically feasible. These results suggest that the proposed framework provides an effective way to connect flow-based trajectory modeling with behavior-aware alignment in dynamic multimedia environments.

---

## 论文详细总结（自动生成）

以下是对论文《TIGFlow-GRPO: Trajectory Forecasting via Interaction-Aware Flow Matching and Reward-Driven Optimization》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在轨迹预测中平衡“统计分布拟合”与“行为规则对齐”。
*   **研究背景**：现有的生成式模型（如扩散模型、流匹配 CFM）虽然能很好地拟合历史数据的分布，但由于主要依赖监督学习，难以直接优化非微分的物理约束（如不进入障碍区）和社会规范（如保持社交距离）。
*   **研究动机**：轨迹预测不仅要预测“统计上可能”的路径，更要确保路径在物理上可行且符合社会礼仪。作者试图引入强化学习（RL）中的对齐技术，将流匹配模型与行为规则相结合。

### 2. 方法论
论文提出了一个名为 **TIGFlow-GRPO** 的两阶段框架：
*   **第一阶段：条件流匹配预训练**
    *   **TIG-GAT 模块**：构建“轨迹交互图”，利用目标中心（Target-Centric）的动态图和视野准则（Field-of-View）筛选相关邻居，通过门控注意力机制（Gated Attention）提取精细的时空交互特征。
    *   **CFM 主干**：学习一个时间相关的向量场，将高斯先验分布转换为目标轨迹分布。
*   **第二阶段：Flow-GRPO 后训练（对齐阶段）**
    *   **ODE 到 SDE 的转换**：标准流匹配是确定性的（ODE），不利于强化学习的探索。作者将其重新表述为随机微分方程（SDE）采样，为 RL 提供探索空间。
    *   **GRPO 优化**：采用组相对策略优化（Group Relative Policy Optimization），通过计算一组生成样本的相对优势来更新策略，无需额外的 Critic 网络，降低了计算开销。
    *   **复合奖励函数**：
        *   **视野感知社会奖励**：根据智能体的朝向和距离，对不同区域（强视野、弱视野、后方）的碰撞风险进行差异化惩罚。
        *   **地图感知语义奖励**：利用预计算的符号距离场（SDF）提供连续的边界反馈，惩罚越界行为。
        *   **准确性与平滑度奖励**：确保预测不偏离基本航向。

### 3. 实验设计
*   **数据集**：
    *   **ETH/UCY**：包含 5 个行人社交场景，侧重于评估社交交互建模。
    *   **SDD (Stanford Drone Dataset)**：校园航拍场景，环境结构复杂，侧重于评估物理边界约束。
*   **Benchmark 与对比方法**：
    *   对比了 8 种主流方法，包括基于图的方法（GroupNet, EqMotion）、扩散模型（MID, SingularTraj）和流匹配模型（MoFlow）。
*   **评估指标**：
    *   位移误差：ADE（平均位移误差）、FDE（最终位移误差）的最小值和平均值。
    *   合规性指标：碰撞率（Col），用于衡量社交合规性。

### 4. 资源与算力
*   **硬件**：使用了单块 **NVIDIA RTX 3090 GPU**。
*   **软件**：基于 PyTorch 实现。
*   **训练细节**：文中提到了预训练使用 AdamW 优化器，学习率为 $1 \times 10^{-4}$；后训练阶段每组采样 $G=4$ 条轨迹进行 GRPO 优化。具体的训练时长未明确说明，但强调了 GRPO 相比传统 RL 方法更节省显存。

### 5. 实验数量与充分性
*   **实验规模**：在 ETH/UCY 的 5 个子集上进行了留一法交叉验证，并在 SDD 上进行了测试，涵盖了从简单到复杂的多种场景。
*   **消融实验**：针对 TIG-GAT 模块、社会奖励、地图奖励以及 GRPO 阶段分别做了消融，验证了各组件的有效性。
*   **客观性**：引入了“平均误差（ADE avg）”和“碰撞率”等指标，相比仅关注“最小误差（min ADE）”的方法，更能客观反映生成分布的整体质量和安全性。

### 6. 主要结论与发现
*   **性能提升**：TIGFlow-GRPO 在 ETH/UCY 和 SDD 上均达到了 SOTA 水平，尤其在平均误差和长时预测稳定性上表现优异。
*   **安全性增强**：通过 Flow-GRPO 对齐，碰撞率在多个数据集上显著下降（如 SDD 从 19.74% 降至 12.29%）。
*   **分布优化**：后训练不仅提升了预测精度，还通过抑制低质量离群样本，使生成的轨迹束更加紧凑且符合逻辑。

### 7. 优点
*   **创新性结合**：首次将 GRPO 这一在大语言模型（LLM）中流行的对齐技术引入到基于流匹配的轨迹预测任务中。
*   **探索机制巧妙**：通过 ODE-to-SDE 的转换解决了流匹配模型在 RL 训练中缺乏随机性的问题。
*   **奖励设计精细**：视野感知的社会奖励和基于 SDF 的地图奖励非常符合人类行为逻辑和物理常识。

### 8. 不足与局限
*   **奖励权重敏感性**：复合奖励包含多个项，权重的超参数调节可能较为繁琐，且不同场景可能需要不同的权重配置。
*   **实时性挑战**：虽然流匹配推理效率高，但后训练阶段的 SDE 采样和优势计算增加了训练复杂度。
*   **输入模态限制**：目前主要依赖坐标和语义地图，尚未充分利用原始视觉像素或多模态大模型（VLM）的语义推理能力（作者在结论中提到这是未来的改进方向）。

（完）
