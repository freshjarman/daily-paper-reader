---
title: "DyMRL: Dynamic Multispace Representation Learning for Multimodal Event Forecasting in Knowledge Graph"
title_zh: DyMRL：面向知识图谱中多模态事件预测的动态多空间表示学习
authors: "Feng Zhao, Kangzheng Liu, Teng Peng, Yu Yang, Guandong Xu"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24636v1"
tags: ["query:tpp-es"]
score: 8.0
evidence: 多模态事件预测与动态结构模态
tldr: 针对多模态知识图谱事件预测中动态知识获取与融合的不足，本文提出DyMRL模型。该模型通过在欧几里得、双曲和复数空间中集成时间敏感的结构特征，并结合预训练模型的视觉与语言智能，实现了深层关系感知特征学习。同时，引入双重融合-演化注意力机制以动态捕捉不同模态的历史贡献。实验表明，DyMRL在四个基准数据集上均优于现有最先进方法。
selection_source: fresh_fetch
motivation: 现有研究多关注静态设置，难以有效获取动态多空间结构信息并捕捉多模态融合特征随时间的演化。
method: 提出DyMRL框架，利用多空间关系消息传递学习深层结构表示，并采用双重融合-演化注意力机制实现动态模态融合。
result: 在四个多模态时序知识图谱基准测试中，DyMRL的性能显著优于现有的动态单模态和静态多模态基准方法。
conclusion: DyMRL通过多空间表示学习和动态融合机制，有效提升了多模态时序知识图谱中的事件预测准确性。
---

## 摘要
准确的多模态知识表示对于现实场景中的事件预测至关重要。然而，现有研究主要集中在静态设置上，忽视了多模态知识的动态获取与融合。1) 在知识获取层面，如何学习不同模态的时敏信息，特别是动态结构模态。现有的动态学习方法通常局限于异构空间或简单单一空间中的浅层结构，难以捕捉深层的关系感知几何特征。2) 在知识融合层面，如何学习演化的多模态融合特征。现有的基于静态协同注意力的知识融合方法难以捕捉不同模态对未来事件随时间变化的历史贡献。为此，我们提出了 DyMRL，一种动态多空间表示学习方法，旨在高效地获取和融合多模态时序知识。1) 针对前一问题，DyMRL 将来自欧几里得空间、双曲空间和复数空间的特定时间结构特征集成到关系消息传递框架中以学习深层表示，反映了人类在联想思维、高阶抽象和逻辑推理方面的智能。预训练模型赋予了 DyMRL 时敏的视觉和语言智能。2) 针对后一问题，DyMRL 结合了先进的双重融合-演化注意力机制，以对称方式在不同时间戳为不同模态分配动态学习权重。为了通过利用历史中学习到的多模态时序知识来评估 DyMRL 的事件预测性能，我们构建了四个多模态时序知识图谱基准。广泛的实验表明，DyMRL 优于最先进的动态单模态和静态多模态基准方法。

## Abstract
Accurate representation of multimodal knowledge is crucial for event forecasting in real-world scenarios. However, existing studies have largely focused on static settings, overlooking the dynamic acquisition and fusion of multimodal knowledge. 1) At the knowledge acquisition level, how to learn time-sensitive information of different modalities, especially the dynamic structural modality. Existing dynamic learning methods are often limited to shallow structures across heterogeneous spaces or simple unispaces, making it difficult to capture deep relation-aware geometric features. 2) At the knowledge fusion level, how to learn evolving multimodal fusion features. Existing knowledge fusion methods based on static coattention struggle to capture the varying historical contributions of different modalities to future events. To this end, we propose DyMRL, a Dynamic Multispace Representation Learning approach to efficiently acquire and fuse multimodal temporal knowledge. 1) For the former issue, DyMRL integrates time-specific structural features from Euclidean, hyperbolic, and complex spaces into a relational message-passing framework to learn deep representations, reflecting human intelligences in associative thinking, high-order abstracting, and logical reasoning. Pretrained models endow DyMRL with time-sensitive visual and linguistic intelligences. 2) For the latter concern, DyMRL incorporates advanced dual fusion-evolution attention mechanisms that assign dynamic learning emphases equally to different modalities at different timestamps in a symmetric manner. To evaluate DyMRL's event forecasting performance through leveraging its learned multimodal temporal knowledge in history, we construct four multimodal temporal knowledge graph benchmarks. Extensive experiments demonstrate that DyMRL outperforms state-of-the-art dynamic unimodal and static multimodal baseline methods.

---

## 论文详细总结（自动生成）

这是一份关于论文《DyMRL: Dynamic Multispace Representation Learning for Multimodal Event Forecasting in Knowledge Graph》的结构化深入分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
论文聚焦于**多模态时序知识图谱（Multimodal Temporal Knowledge Graph, MTKG）中的事件预测**。
*   **研究动机**：现实世界中的事件不仅具有时序性，还包含丰富的多模态信息（如文本描述、图像）。然而，现有研究存在两大局限：
    1.  **知识获取层面**：现有的动态学习方法多局限于单一几何空间或浅层结构，难以捕捉深层的、具备关系感知的几何特征（如层级结构、逻辑推理模式）。
    2.  **知识融合层面**：现有的多模态融合多基于静态协同注意力（Co-attention），无法捕捉不同模态在不同历史时间戳对未来事件贡献的演化过程。
*   **核心目标**：模拟人类的多智能认知范式（联想思维、高阶抽象、逻辑推理、视觉与语言智能），提出一种能高效获取并融合动态多模态知识的表示学习框架。

### 2. 方法论：核心思想与关键技术
论文提出了 **DyMRL** 模型，其核心架构分为三个主要模块：
*   **动态结构模态获取（Dynamic Structural Modality Acquisition）**：
    *   **多空间消息传递**：在关系消息传递框架中集成三种几何空间：**欧几里得空间**（捕捉局部联想特征）、**双曲空间**（利用负曲率捕捉高阶层级抽象）和**复数空间**（利用球壳几何嵌入对称/传递等逻辑规律）。
    *   **深层传播与更新**：通过多层图神经网络（GNN）将浅层几何特征扩展到深层结构，并使用循环神经网络（RNN）作为更新模块，捕捉结构随时间的演化。
*   **动态辅助模态获取（Dynamic Auxiliary Modality Acquisition）**：
    *   利用预训练的 **VGG**（视觉）和 **BERT**（语言）模型提取时间敏感的图像和文本特征，并同样通过 RNN 模块建模其时序变化。
*   **双重融合-演化注意力机制（Dual Fusion-Evolution Attention）**：
    *   **融合注意力**：在每个时间戳内，以初始嵌入矩阵作为第三方“分配者”，动态权衡结构、视觉、语言三种模态的贡献。
    *   **演化注意力**：在时间维度上，进一步分配不同历史时间戳的权重，提取对未来预测最有价值的演化模式。
*   **解码与训练**：采用曲率自适应的距离函数进行评分，并通过交叉熵损失函数进行多标签学习。

### 3. 实验设计
*   **数据集**：构建了四个多模态时序知识图谱基准数据集：**GDELT-IMG-TXT**、**ICE14-IMG-TXT**、**ICE0515-IMG-TXT** 和 **ICE18-IMG-TXT**。这些数据集在传统时序 KG 基础上补充了从 Google Images 爬取的图像和相关的文本描述。
*   **对比方法（Baselines）**：
    *   **静态多模态方法**：如 TransAE、MoSE、OTKGE、IMF、DySarl。
    *   **动态单模态方法**：如 xERTE、TiRGN、RE-GCN、CENET、RPC、ReTIN、CognTKE 等。
*   **评估指标**：MRR（平均倒数排名）、Hits@1、Hits@10，采用时间感知过滤（Time-aware filtered）设置。

### 4. 资源与算力
*   **硬件环境**：使用单张 **NVIDIA Tesla A100 GPU (80 GB)**。
*   **软件环境**：Ubuntu Linux，PyTorch 框架。
*   **训练细节**：历史训练轮数为 400 轮，设置了 100 轮的早停（Early Stopping）机制。
*   **效率表现**：论文专门对比了推理时间，DyMRL 在处理大规模查询时比基于规则的 CognTKE 快 7-73 倍，展现了良好的并行计算效率。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 4 个数据集上进行了全面的性能对比。
    *   **消融实验**：设计了 11 组变体（如移除不同空间消息、移除不同模态、移除注意力机制等），验证了各组件的必要性。
    *   **敏感性分析**：探讨了历史窗口长度 $k$ 对结果的影响。
    *   **可视化分析**：通过热力图展示了不同模态在不同时间戳的注意力权重分布。
*   **客观性与公平性**：每组实验均独立运行 10 次并取平均值；使用了启发式搜索（Hyperopt）进行超参数优化；对比了最新的 2024/2025 年基准方法，实验设计较为严谨、充分。

### 6. 主要结论与发现
*   **性能领先**：DyMRL 在所有数据集和指标上均显著优于现有的静态多模态和动态单模态方法。
*   **多空间价值**：双曲空间在捕捉层级结构方面贡献最大，多空间集成优于任何单一空间。
*   **模态贡献**：结构模态贡献最高，语言模态次之，视觉模态相对较弱。
*   **时序特性**：距离未来事件越近的历史时间戳，其提供的预测价值越高。

### 7. 优点
*   **创新性强**：首次在动态场景下同时考虑了多空间几何结构学习与多模态特征演化。
*   **认知对齐**：模型设计参考了人类的多智能认知模式，具有较好的直观解释性。
*   **架构对称**：双重注意力机制设计优雅，解决了传统协同注意力无法处理时序演化权重的问题。
*   **资源贡献**：构建并开源了四个高质量的 MTKG 基准数据集，填补了领域空白。

### 8. 不足与局限
*   **模型复杂度**：由于涉及多空间计算和多个预训练模型特征提取，其计算开销高于纯单模态模型（如 ReTIN）。
*   **数据噪声风险**：辅助模态数据（图像）是通过爬虫获取的，可能存在语义漂移或噪声，论文未深入讨论如何过滤这些噪声。
*   **应用限制**：模型依赖于高质量的辅助模态信息，在缺乏文本或图像描述的稀疏知识图谱中，性能提升可能受限。

（完）
