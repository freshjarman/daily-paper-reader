---
title: Kronecker-Structured Nonparametric Spatiotemporal Point Processes
title_zh: 克罗内克结构非参数时空点过程
authors: "Zhitong Xu, Qiwei Yuan, Yinghao Chen, Yan Sun, Bin Shen, Shandian Zhe"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23746v1"
tags: ["query:tpp-es"]
score: 10.0
evidence: 用于事件预测的克罗内克结构非参数时空点过程
tldr: 本文针对传统参数化时空点过程（如泊松和霍克斯过程）灵活性不足、以及近期神经点过程模型可解释性差的问题，提出了一种克罗内克结构的非参数时空点过程（KSTPP）。该模型使用高斯过程分别建模背景强度和影响核函数，能捕捉包括激发、抑制、中性和时变效应在内的丰富交互模式。通过采用可分离乘积核和在结构化网格上表示高斯过程，利用克罗内克协方差矩阵及张量积高斯-勒让德求积方案，实现了大规模事件集合的高效可扩展训练与预测。实验验证了该框架的有效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.23746v1/fig-001.webp\", \"caption\": \"Table 3: Root-mean-square error (RMSE) and Euclidean distance for next-event time and location prediction. The best two results are highlighted in bold.\", \"page\": 11, \"index\": 1, \"width\": 806, \"height\": 256}, {\"url\": \"assets/figures/arxiv/2603.23746v1/fig-002.webp\", \"caption\": \"Figure 4: Learned influence kernel by KSTPP on Earthquake.\", \"page\": 11, \"index\": 2, \"width\": 664, \"height\": 665}, {\"url\": \"assets/figures/arxiv/2603.23746v1/fig-003.webp\", \"caption\": \"Figure 5: Temporal conditional intensity on example test sequences from SYN2.\", \"page\": 15, \"index\": 3, \"width\": 402, \"height\": 303}, {\"url\": \"assets/figures/arxiv/2603.23746v1/fig-004.webp\", \"caption\": \"Figure 3: Learned influence kernel from SYN1.\", \"page\": 10, \"index\": 4, \"width\": 826, \"height\": 831}, {\"url\": \"assets/figures/arxiv/2603.23746v1/fig-005.webp\", \"caption\": \"Figure 2: Spatial conditional intensity p(x, y | t,Ht) = λ(t, x, y | Ht)/λ(t | Ht) on an example test sequence from SYN1. GT denotes the ground-truth.\", \"page\": 9, \"index\": 5, \"width\": 820, \"height\": 407}, {\"url\": \"assets/figures/arxiv/2603.23746v1/fig-006.webp\", \"caption\": \"Figure 6: Spatial conditional intensities on an example test sequence from SYN2 at different time points.\", \"page\": 16, \"index\": 6, \"width\": 820, \"height\": 406}, {\"url\": \"assets/figures/arxiv/2603.23746v1/fig-007.webp\", \"caption\": \"Figure 7: Learned influence kernel from SYN2 dataset.\", \"page\": 16, \"index\": 7, \"width\": 414, \"height\": 419}]"
motivation: 现有参数化时空点过程模型灵活性有限，而神经点过程模型则缺乏可解释性，需要一种既能发现透明事件关系又具有高建模灵活性的方法。
method: 提出KSTPP模型，使用高斯过程建模背景强度和影响核，并利用可分离乘积核与结构化网格上的克罗内克协方差矩阵实现可扩展训练。
result: 大量实验证明了该框架的有效性，其克罗内克代数显著降低了计算成本，使模型能够扩展到大型事件集合。
conclusion: Kronecker-Structured Nonparametric Spatiotemporal Point Processes (KSTPP) 通过结合非参数高斯过程与克罗内克结构，在保持高灵活性的同时实现了可解释的关系发现和高效计算。
---

## 摘要
时空领域的事件在众多现实应用中频繁出现，其中揭示事件关系并实现准确预测是核心挑战。经典的泊松过程和霍克斯过程依赖于限制性的参数假设，这限制了它们捕捉复杂交互模式的能力；而最近的神经点过程模型虽然提升了表示能力，却以黑盒方式整合事件信息，阻碍了可解释关系的发现。为应对这些局限，我们提出了一种克罗内克结构非参数时空点过程（KSTPP），该模型能够在保持高度建模灵活性的同时实现透明的事件级关系发现。我们使用空间高斯过程（GP）对背景强度进行建模，并将影响核建模为时空高斯过程，从而允许包括激发、抑制、中立和时变效应在内的丰富交互模式。为实现可扩展的训练与预测，我们采用可分离的乘积核函数并在结构化网格上表示高斯过程，从而诱导出克罗内克结构的协方差矩阵。利用克罗内克代数显著降低了计算成本，并使模型能够扩展到大规模事件集合。此外，我们开发了一种张量积高斯-勒让德求积方案来高效评估难以处理的似然积分。大量实验证明了我们框架的有效性。

## Abstract
Events in spatiotemporal domains arise in numerous real-world applications, where uncovering event relationships and enabling accurate prediction are central challenges. Classical Poisson and Hawkes processes rely on restrictive parametric assumptions that limit their ability to capture complex interaction patterns, while recent neural point process models increase representational capacity but integrate event information in a black-box manner, hindering interpretable relationship discovery. To address these limitations, we propose a Kronecker-Structured Nonparametric Spatiotemporal Point Process (KSTPP) that enables transparent event-wise relationship discovery while retaining high modeling flexibility. We model the background intensity with a spatial Gaussian process (GP) and the influence kernel as a spatiotemporal GP, allowing rich interaction patterns including excitation, inhibition, neutrality, and time-varying effects. To enable scalable training and prediction, we adopt separable product kernels and represent the GPs on structured grids, inducing Kronecker-structured covariance matrices. Exploiting Kronecker algebra substantially reduces computational cost and allows the model to scale to large event collections. In addition, we develop a tensor-product Gauss-Legendre quadrature scheme to efficiently evaluate intractable likelihood integrals. Extensive experiments demonstrate the effectiveness of our framework.

---

## 论文详细总结（自动生成）

好的，作为一名资深学术论文分析助手，我将对这篇题为《Kronecker-Structured Nonparametric Spatiotemporal Point Processes》的论文进行结构化、深入且客观的总结。

# Kronecker-Structured Nonparametric Spatiotemporal Point Processes 论文分析总结

## 1. 核心问题与整体含义
*   **研究动机**：时空点过程建模在天气、地震、流行病传播等领域至关重要。现有方法存在明显局限：
    *   **经典参数化模型（如泊松过程、霍克斯过程）**：假设过于严格（如事件独立或仅激发效应），无法灵活捕捉复杂的时空交互模式（如抑制效应）。
    *   **神经点过程模型**：虽然表示能力强，但以“黑盒”方式编码事件历史，缺乏可解释性，难以揭示明确的事件间关系。
*   **整体目标**：提出一个既能保持高建模灵活性（捕捉激发、抑制、中性等复杂模式），又能实现**透明、可解释的事件级关系发现**的时空点过程框架。

## 2. 提出的方法论
*   **核心思想**：将条件强度函数建模为背景强度与历史事件影响的叠加，并对两个核心组件采用非参数化的高斯过程先验。
    *   **模型定义**：
        `λ(t, x, y | H_t) = σ( g(x, y) + Σ_{t_n < t} f(t - t_n, x - x_n, y - y_n) )`
        *   `g(x, y)`：空间高斯过程 (GP)，建模自发事件的背景强度。
        *   `f(Δt, Δx, Δy)`：时空高斯过程 (GP)，作为影响核函数，其值可正（激发）、负（抑制）或接近零（中性）。
        *   `σ(·)`：SoftPlus链接函数，确保强度为正。
*   **关键技术细节**：
    1.  **克罗内克结构实现可扩展性**：
        *   为`g`和`f`设计**可分离的乘积核函数**（例如，`κ(Δt, Δx, Δy) = κ_t(Δt)·κ_x(Δx)·κ_y(Δy)`）。
        *   在结构化网格上定义诱导点集。这使得协方差矩阵具有克罗内克积结构（如 `K = K_t ⊗ K_x ⊗ K_y`）。
        *   利用克罗内克代数，将大规模矩阵运算分解为各维度小矩阵的运算，将计算复杂度从 `O(m^3)` 降至 `O(Σ m_k^3)` （`m_k`为单维度网格大小），实现了对大规模事件数据的高效处理。
    2.  **高效似然计算方案**：
        *   点过程的似然包含难以解析计算的积分项。
        *   提出一种**张量积高斯-勒让德求积法**。利用影响核和背景函数的可分离结构，在结构化求积网格上高效评估GP函数值，并通过加权求和快速计算高维积分。
    3.  **预测方法**：
        *   **下一事件时间预测**：基于条件密度公式，通过变量变换将无穷积分映射到[0,1]区间，再利用高斯-勒让德求积进行计算。
        *   **下一事件位置预测**：在给定预测时间下，计算空间坐标的条件期望值。

## 3. 实验设计
*   **数据集与场景**：
    1.  **合成数据 (SYN1 & SYN2)**：用于验证模型恢复真实强度和识别激发/抑制模式的能力。SYN1根据时间滞后切换激发/抑制；SYN2根据空间距离切换。
    2.  **真实世界基准数据集**：
        *   **Earthquake (地震)**：日本1990-2020年的地震序列数据。
        *   **Covid-19 (新冠疫情)**：美国新泽西州各县的每日病例数据。
        *   **Citibike (共享单车)**：纽约市共享单车出行记录数据。
*   **对比方法 (Benchmark)**：
    *   **经典/参数化模型**: Spatiotemporal Hawkes Process (STHP), Homogeneous Poisson Process (PP)
    *   **神经点过程模型**: Neural Spatiotemporal Point Process (NSTPP), Deep Spatiotemporal Hawkes Process (DeepSTPP), Neural Jump Stochastic Differential Equations (NJSDE), Neural Hawkes Process (NHP), Transformer Hawkes Process (THP), Recurrent Marked Temporal Point Processes (RMTPP)
    *   **生成式模型**: Diffusion Spatiotemporal Point Process (DSTPP)

## 4. 资源与算力
*   论文中未明确说明训练所使用的具体硬件配置（如GPU型号、数量）、训练时长或算力消耗。仅提及方法在PyTorch中实现，并使用Adam优化器进行训练。

## 5. 实验数量与充分性
*   **实验数量充足且设计全面**：
    1.  **合成数据实验**: 
        *   验证了强度恢复能力（报告相对L2误差并可视化）。
        *   验证了影响核估计能力（可视化学习到的影响核并与真实情况对比）。
    2.  **真实数据预测性能实验**: 
        *   在三个不同领域的基准数据集上评估了下一事件时间和位置的预测误差（RMSE和欧氏距离）。
    3.  **模式发现分析**: 
        *   在地震数据集上可视化学习到的影响核随时间和空间的变化，并给出了符合领域知识（如Omori-Utsu定律、应力影区）的物理解释。
*   **客观性与公平性评估**:
    *   对比了涵盖经典、神经和最新生成式方法的广泛基线。
    *   对于部分基线方法的结果直接引用了原论文报告的最优结果以确保公平比较。
    *   使用了标准的训练/验证/测试集划分（遵循对比论文的设置）。
    *   每个实验重复运行多次并报告均值和标准差。

## 6. 主要结论与发现
1.  KSTPP在合成数据上能最准确地恢复真实的时空条件强度函数，并能成功识别出数据中预设的激发和抑制模式。
2. KSTPP在所有三个真实世界数据集的下一次事件预测任务中均取得了领先或极具竞争力的性能。尤其在位置预测上表现最佳；在时间预测上与性能最好的扩散模型DSTPP相当或接近。
3. KSTPP学习到的影响核具有明确的物理意义。例如在地震数据中，它自动发现了短时近距离的激发效应和可能的局部抑制效应（“应力影区”），这与地震学原理相符。

## 7. 优点
*   **兼顾灵活性与可解释性**: GP的非参数特性赋予了模型捕捉复杂模式的灵活性；而加性结构 (`g + Σf`)使得每个历史事件的贡献清晰可见 (`f`的值直接解释了其影响)。
*   ****创新的高效算法****: 
		*	结合克罗内克结构与诱导点表示法解决了GP在大规模时空点过程中的计算瓶颈问题；
		*	设计的张量积求积方案高效解决了似然中的难解积分问题；
		*	整套算法实现了从训练到推理的可扩展性；
		
		这些技术贡献是本文的核心创新之一；
		
		

## .8不足与局限
*

*   **模型假设的潜在限制**：模型的核心结构（加性背景与历史影响）和可分离核假设虽然带来了计算优势，但可能无法捕捉某些更复杂的、非可分离的或非加性的时空依赖模式。这在一定程度上限制了模型的完全通用性。
*   **对结构化网格的依赖**：诱导点必须放置在规则的时空网格上以实现克罗内克结构。这要求对数据域进行离散化，其分辨率（网格粒度）是一个需要选择或调优的超参数，可能影响模型精度和计算效率的平衡。
*   **与深度生成模型的比较**：尽管在预测任务上表现优异，但论文未将KSTPP作为生成模型（即从模型中采样完整序列）的能力与DSTPP等先进生成式方法进行系统比较。其采样效率和生成序列的质量尚不明确。

## 9. 未来工作方向
*   **探索更灵活的核结构**：可以研究如何将克罗内克结构的效率优势与非平稳、非可分离的核函数相结合，以进一步提升模型的表达能力。
*   **扩展到更复杂的标记**：当前模型处理的是基本的时空坐标。一个自然的扩展是将其推广到包含更多元信息的标记点过程（例如，地震震级、疾病类型），通过为标记维度也引入高斯过程或神经网络组件。
*   **集成领域知识**：由于模型具有可解释性，未来工作可以探索如何将部分已知的物理约束（如特定形式的衰减定律）作为先验知识融入到高斯过程的均值或核函数中，构建物理信息驱动的混合模型。

## 10. 总结评价
本文提出了一种新颖且实用的时空点过程建模框架KSTPP。它通过巧妙地将非参数高斯过程与克罗内克代数结构相结合，在保持模型高度灵活性的同时，成功解决了传统方法在可解释性和计算可扩展性方面的核心痛点。实验表明，该方法不仅在预测精度上与最先进的“黑盒”神经网络方法相当甚至更优，更重要的是能够提供清晰、符合领域直觉的事件间相互作用模式的可视化与解释。尽管存在对结构化假设和网格设置的依赖等局限，但其在效率、性能与透明度之间取得的平衡使其成为时空事件数据分析中一个极具价值的工具，并为后续结合领域知识与复杂建模的研究开辟了道路。

（完）
