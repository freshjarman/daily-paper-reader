---
title: Kronecker-Structured Nonparametric Spatiotemporal Point Processes
title_zh: Kronecker 结构的非参数化时空点过程
authors: "Zhitong Xu, Qiwei Yuan, Yinghao Chen, Yan Sun, Bin Shen, Shandian Zhe"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23746v1"
tags: ["query:tpp-es"]
score: 10.0
evidence: 用于事件预测的非参数时空点过程
tldr: 针对时空点过程模型在捕捉复杂交互模式与可解释性之间的矛盾，本文提出了一种克罗内克结构非参数时空点过程（KSTPP）。该模型利用空间高斯过程建模背景强度，并以时空高斯过程作为影响核，实现了对激励、抑制等多种交互模式的透明建模。通过引入可分离乘积核与克罗内克代数，显著降低了计算复杂度，使其能扩展至大规模数据集，在保持高灵活性的同时实现了可解释的关系发现。
selection_source: fresh_fetch
motivation: 传统点过程模型受限于参数化假设，而神经点过程模型缺乏可解释性，难以揭示复杂的事件间关系。
method: 采用高斯过程分别建模背景强度和影响核，并利用克罗内克结构协方差矩阵与张量积高斯-勒让德求积法提升计算效率。
result: 实验结果表明，该框架在处理大规模事件集合时具有出色的建模灵活性和计算效率。
conclusion: KSTPP 有效结合了非参数模型的灵活性与结构化计算的高效性，为时空事件关系发现提供了透明且可扩展的解决方案。
---

## 摘要
时空域中的事件出现在众多现实应用中，其中揭示事件关系并实现准确预测是核心挑战。经典的泊松过程和霍克斯过程依赖于限制性的参数假设，这限制了它们捕捉复杂交互模式的能力；而近期的神经点过程模型虽然提升了表示能力，但以黑盒方式整合事件信息，阻碍了可解释的关系发现。为了解决这些局限性，我们提出了一种 Kronecker 结构的非参数化时空点过程（KSTPP），它在保持高建模灵活性的同时，实现了透明的事件级关系发现。我们将背景强度建模为空间高斯过程（GP），并将影响核建模为时空高斯过程，从而支持丰富的交互模式，包括激励、抑制、中性和随时间变化的效应。为了实现可扩展的训练和预测，我们采用了可分离乘积核，并在结构化网格上表示高斯过程，从而导出 Kronecker 结构的协方差矩阵。利用 Kronecker 代数显著降低了计算成本，并使模型能够扩展到大规模事件集合。此外，我们开发了一种张量积高斯-勒让德求积方案，以高效评估难以处理的似然积分。广泛的实验证明了我们框架的有效性。

## Abstract
Events in spatiotemporal domains arise in numerous real-world applications, where uncovering event relationships and enabling accurate prediction are central challenges. Classical Poisson and Hawkes processes rely on restrictive parametric assumptions that limit their ability to capture complex interaction patterns, while recent neural point process models increase representational capacity but integrate event information in a black-box manner, hindering interpretable relationship discovery. To address these limitations, we propose a Kronecker-Structured Nonparametric Spatiotemporal Point Process (KSTPP) that enables transparent event-wise relationship discovery while retaining high modeling flexibility. We model the background intensity with a spatial Gaussian process (GP) and the influence kernel as a spatiotemporal GP, allowing rich interaction patterns including excitation, inhibition, neutrality, and time-varying effects. To enable scalable training and prediction, we adopt separable product kernels and represent the GPs on structured grids, inducing Kronecker-structured covariance matrices. Exploiting Kronecker algebra substantially reduces computational cost and allows the model to scale to large event collections. In addition, we develop a tensor-product Gauss-Legendre quadrature scheme to efficiently evaluate intractable likelihood integrals. Extensive experiments demonstrate the effectiveness of our framework.

---

## 论文详细总结（自动生成）

### 论文结构化深入总结：Kronecker-Structured Nonparametric Spatiotemporal Point Processes

#### 1. 核心问题与整体含义（研究动机和背景）
论文关注的是**时空点过程（Spatiotemporal Point Processes, STPP）**的建模问题。其核心挑战在于如何在保持模型**高灵活性**（捕捉复杂的激励、抑制和时变效应）的同时，确保模型的**可解释性**（显式发现事件间的关系）和**计算可扩展性**。
*   **背景：** 经典模型（如泊松过程、霍克斯过程）依赖强参数假设，难以捕捉复杂模式；现代神经点过程模型（如 Neural Hawkes）虽表达力强，但属于“黑盒”模型，难以直观展示事件间的相互影响。
*   **动机：** 开发一种既能像非参数模型一样灵活，又能像经典模型一样提供透明交互关系，且能处理大规模数据集的新框架。

#### 2. 方法论：核心思想与技术细节
论文提出了 **KSTPP（Kronecker-Structured Nonparametric Spatiotemporal Point Process）**，其核心架构如下：
*   **强度函数建模：** 条件强度函数 $\lambda(t, s | \mathcal{H}_t)$ 被定义为背景强度 $g(x, y)$ 与历史事件影响核 $f(\Delta t, \Delta x, \Delta y)$ 叠加后的非线性变换（使用 SoftPlus 函数确保正值）。
*   **非参数先验：** 对背景函数 $g$ 施加空间高斯过程（GP）先验，对影响核 $f$ 施加时空高斯过程先验。这允许 $f$ 取正值（激励）、负值（抑制）或零（无影响）。
*   **Kronecker 结构加速：** 为解决 GP 在大规模数据下 $O(N^3)$ 的计算复杂度，模型采用了**可分离乘积核**（Separable Product Kernels）并在结构化网格上定义诱导点。这使得协方差矩阵具有 Kronecker 积结构，利用 Kronecker 代数将复杂度降至各维度网格大小的立方级，极大提升了扩展性。
*   **张量积高斯-勒让德求积：** 针对似然函数中难以处理的积分项，提出了一种基于张量积的高斯-勒让德求积方案，利用相同的 Kronecker 结构高效评估时空积分。

#### 3. 实验设计
*   **数据集：**
    *   **合成数据（SYN1, SYN2）：** 专门设计用于验证模型捕捉“时间滞后抑制”和“局部空间抑制”的能力。
    *   **真实数据：** Earthquake（日本地震及余震）、Covid-19（新泽西州县级病例）、Citibike（纽约共享单车记录）。
*   **Benchmark 与对比方法：**
    *   **经典/参数化模型：** 泊松过程（PP）、时空霍克斯过程（STHP）。
    *   **神经点过程：** NSTPP、DeepSTPP、NHP、THP、RMTPP、NJSDE。
    *   **生成式模型：** 基于扩散模型的 DSTPP（目前的最强基准之一）。
*   **评估指标：** 强度恢复的相对 $L_2$ 误差、下一次事件预测的时间 RMSE 和空间欧几里得距离。

#### 4. 资源与算力
*   **软件环境：** 使用 PyTorch 实现，采用 Adam 优化器。
*   **硬件说明：** 论文**未明确指出**具体的 GPU 型号、数量或具体的训练时长。但从算法复杂度分析来看，由于引入了 Kronecker 加速，该模型在普通工作站级别硬件上应具备处理数千个事件序列的能力。

#### 5. 实验数量与充分性
*   **实验规模：** 涵盖了合成实验和三个不同领域的真实数据集，每组实验重复运行 5 次并报告均值和标准差。
*   **充分性：** 实验设计较为全面。不仅对比了预测精度，还通过合成实验验证了强度函数恢复的准确性，并通过地震数据的可视化展示了模型发现“应力影子（stress shadows）”等物理现象的能力。
*   **公平性：** 对比方法均采用了官方开源实现或已发表的最优超参数，确保了对比的客观性。

#### 6 主要结论与发现
*   **预测性能：** KSTPP 在下一次事件的时间和位置预测上一致优于大多数神经点过程模型，且达到了与复杂的扩散模型（DSTPP）相当的水平。
*   **模式发现：** KSTPP 能够准确识别出传统霍克斯过程无法捕捉的**抑制效应**（Inhibition），这在地震学和流行病学建模中具有重要意义。
*   **计算效率：** 通过 Kronecker 结构，模型成功处理了包含大量事件的序列，证明了非参数 GP 方法在点过程大规模应用中的可行性。

#### 7. 优点（亮点）
*   **平衡性：** 成功在非参数模型的灵活性与经典模型的可解释性之间找到了平衡点。
*   **数学优雅性：** 利用 Kronecker 代数和张量积求积法解决了 GP 建模中的计算瓶颈，技术路径清晰且高效。
*   **透明度：** 影响核 $f$ 的可视化直接揭示了事件间的时空交互逻辑，优于黑盒神经模型。

#### 8. 不足与局限
*   **空间域限制：** 目前假设空间域为矩形（为了构建结构化网格），对于极不规则的地理边界可能需要额外的坐标变换或掩码处理。
*   **超参数敏感性：** 诱导点网格的密度和求积节点的数量对精度和速度有直接影响，论文未深入讨论如何自动选择这些超参数。
*   **维度扩展：** 虽然提到可以扩展到 3D 空间，但随着维度增加，Kronecker 结构的存储压力仍会增大（尽管是线性的）。

（完）
