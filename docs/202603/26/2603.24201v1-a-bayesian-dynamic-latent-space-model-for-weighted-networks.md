---
title: A Bayesian Dynamic Latent Space Model for Weighted Networks
title_zh: 一种用于加权网络的贝叶斯动态潜空间模型
authors: "Roberto Casarin, Matteo Iacopini, Antonio Peruzzi"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24201v1"
tags: ["query:tpp-es"]
score: 9.0
evidence: 时间网络的点过程表示
tldr: 本研究提出了一种针对加权动态网络的贝叶斯动态潜空间特征模型（LSM）。该模型能够处理整数权重、零膨胀及随时间变化的节点特征和网络稀疏性。通过引入向量自回归过程捕捉节点间的时空依赖，并利用高效的辅助混合采样器和多步采样策略，解决了潜空间维度选择和推断难题，显著提升了计算效率和模型泛化能力。
selection_source: fresh_fetch
motivation: 现有的潜空间模型往往忽略了节点特征间的滞后和同期依赖关系，且在处理加权动态网络时面临推断复杂和维度选择困难的问题。
method: 提出一种基于向量自回归过程的动态潜空间模型，并结合辅助混合采样器、多步采样策略及拉普拉斯近似进行贝叶斯推断。
result: 该方法实现了对潜特征轨迹的高效块采样，显著减少了计算时间并改善了马尔可夫链的混合性能。
conclusion: 该程序具有高度通用性，可灵活应用于静态或动态网络，并支持多种离散或连续的权重分布。
---

## 摘要
针对加权时序网络，本文提出了一种新型动态潜空间特征模型（LSM）。该模型能够处理整数值权重、零值过多、随时间变化的节点位置（特征）以及随时间变化的网络稀疏性。潜位置根据向量自回归过程演化，该过程考虑了节点和特征之间的滞后及同期依赖性，而这一特性在 LSM 文献中常被忽视。本文采用贝叶斯方法来解决动态 LSM 推断中两个主要的难点：潜特征估计和潜空间维度的选择。我们采用了一种高效的辅助混合采样器，该采样器执行数据增强并支持条件共轭先验分布。利用网络权重的点过程表示和潜过程的有限维分布，推导出一种多步（multi-move）采样器，其中每个特征轨迹都在单个块中抽取，无需递归。这种采样策略在网络文献中是新颖的，可以显著减少计算时间，同时改善马尔可夫链的混合性。为了避免跨维度采样器，本文使用部分边缘似然的拉普拉斯近似来设计部分折叠 Gibbs 采样器。总的来说，我们的程序具有通用性，可以轻松适应静态和动态设置，以及其他离散或连续的权重分布。

## Abstract
A new dynamic latent space eigenmodel (LSM) is proposed for weighted temporal networks. The model accommodates integer-valued weights, excess of zeros, time-varying node positions (features), and time-varying network sparsity. The latent positions evolve according to a vector autoregressive process that accounts for lagged and contemporaneous dependence across nodes and features, a characteristic neglected in the LSM literature. A Bayesian approach is used to address two of the primary sources of inference intractability in dynamic LSMs: latent feature estimation and the choice of latent space dimension. We employ an efficient auxiliary-mixture sampler that performs data augmentation and supports conditionally conjugate prior distributions. A point-process representation of the network weights and the finite-dimensional distribution of the latent processes are used to derive a multi-move sampler in which each feature trajectory is drawn in a single block, without recursions. This sampling strategy is new to the network literature and can significantly reduce computational time while improving chain mixing. To avoid trans-dimensional samplers, a Laplace approximation of the partial marginal likelihood is used to design a partially collapsed Gibbs sampler. Overall, our procedure is general, as it can be easily adapted to static and dynamic settings, as well as to other discrete or continuous weight distributions.

---

## 论文详细总结（自动生成）

这篇论文提出了一种用于加权动态网络的贝叶斯动态潜空间特征模型（LSM），旨在解决复杂网络数据中的权重建模、零膨胀处理及计算效率问题。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：现有的潜空间模型（LSM）大多针对静态二元网络。然而，现实世界中的网络（如贸易、大脑连接）通常是**动态的、加权的（整数计数）**，且包含大量的**结构性零值**（零膨胀）。
*   **核心挑战**：在动态背景下，估计潜特征轨迹、处理非高斯似然（如泊松分布）以及确定潜空间的最优维度 $d$ 具有极高的计算复杂度和推断难度。
*   **整体含义**：本文通过引入一种新型的特征模型（Eigenmodel）框架，配合高效的贝叶斯推断算法，实现了对加权时序网络中节点演化、群体聚类及稀疏性的深度刻画。

### 2. 方法论
*   **模型构建**：
    *   **零膨胀泊松特征模型**：观测到的边权重由一个二元分量（决定是否存在连接）和一个泊松分量（决定连接强度）组成。强度由节点个体效应 $\alpha$ 和潜特征向量 $x$ 的内积决定。
    *   **动态演化**：潜特征矩阵 $X_t$ 遵循矩阵自回归过程。论文提出了“特征维度（Feature-wise）”参数化方案，允许节点间存在同期依赖性，这比传统的独立性假设更符合现实。
*   **关键技术细节**：
    *   **辅助混合采样器（IAMS）**：通过两次数据增强，将非线性的泊松似然转化为条件线性高斯模型，从而可以使用共轭先验。
    *   **AWOL（无循环采样）策略**：采用多步（Multi-move）采样器，一次性抽取整个特征轨迹块，避免了传统算法中的时间递归，显著提升了马尔可夫链（MCMC）的混合效率。
    *   **维度选择**：设计了一个**部分折叠 Gibbs 采样器（PCG）**，利用拉普拉斯近似（Laplace Approximation）估计边缘似然，从而在不使用复杂的跨维度采样（如 RJMCMC）的情况下自动推断潜空间维度 $d$。

### 3. 实验设计
*   **数据集与场景**：
    *   **模拟实验**：验证参数恢复的准确性、模型对错误指定的鲁棒性，以及在不同样本量和零值比例下的表现。
    *   **联合国投票网络（UN Co-voting）**：分析 2004-2024 年间 50 个国家的投票一致性，识别地缘政治集团（如北约与金砖国家）的演变。
    *   **国际贸易网络（Trade Networks）**：研究 50 个主要经济体间贸易产品种类的多样性，捕捉地缘政治冲突（如俄乌冲突）对贸易地位的影响。
    *   **大脑结构连接网络（Brain Networks）**：利用 DTI 影像数据，分析 200 个脑区的白质纤维连接，探讨左右半球的不对称性及结构性零值的分布。
*   **Benchmark**：对比了传统的单步 Metropolis-Hastings (MH) 采样器，重点评估有效样本量（ESS）和计算耗时。

### 4. 资源与算力
*   **说明情况**：论文**未明确说明**具体的硬件型号（如 GPU/CPU 核心数）或总训练时长。
*   **效率评价**：作者强调了 AWOL 采样策略和 IAMS 方案在减少计算时间方面的优势，指出该方法比现有的动态 LSM 推断方法更具扩展性，尤其是在处理 $T$ 较大或 $N$ 较多的网络时。

### 5. 实验数量与充分性
*   **实验规模**：进行了多组模拟实验（涵盖不同维度、样本量和稀疏度）以及三个跨领域的真实应用案例。
*   **充分性评价**：实验设计较为充分且客观。通过模拟实验验证了统计性质，通过真实案例展示了模型的解释力。特别是对维度 $d$ 的后验估计和对结构性零值的识别，体现了模型在处理复杂噪声数据时的稳健性。

### 6. 主要结论与发现
*   **模型效能**：新模型能准确捕捉动态网络中的社区结构和节点位置的细微漂移。
*   **维度推断**：通过 PCG 采样器可以有效地确定潜空间维度，例如在联合国投票数据中发现 $d=2$ 是最合适的。
*   **应用发现**：
    *   在贸易网络中，成功识别出俄罗斯和伊朗因地缘政治压力而偏离核心贸易圈的轨迹。
    *   在大脑网络中，证实了左半球比右半球具有更高的连接中心性，并区分了真正的解剖学缺失与测量噪声。

### 7. 优点
*   **计算创新**：AWOL 块采样和 IAMS 数据增强的结合，解决了动态泊松回归中长期存在的计算瓶颈。
*   **灵活性**：支持零膨胀处理，且能自动推断潜空间维度，无需预先设定或依赖信息准则（如 AIC/BIC）进行后验筛选。
*   **依赖建模**：特征维度参数化允许捕捉节点间的空间/文化相关性，填补了 LSM 文献的空白。

### 8. 不足与局限
*   **计算扩展性**：尽管比传统方法快，但对于超大规模网络（如 $N > 1000$），高维精度矩阵的估计（即使使用了图形马蹄先验）仍可能面临内存和计算压力。
*   **近似风险**：维度选择依赖于拉普拉斯近似，在后验分布极度非对称或多峰的情况下，近似精度可能会下降。
*   **参数敏感性**：虽然声称稳健，但图形马蹄先验（Graphical Horseshoe）的超参数设置对高维协方差矩阵的稀疏性诱导仍有一定影响。

（完）
